## v1.8.11

优动漫 PAINT 的副色槽、透明色板与「当前槽位」跟着 CSP 一起同步了；「取色后点画布第一击没反应」这条线又挖出一处真 bug——上一版为保护 WinTab 加的「零搅动」判断，因为调了一个 pywin32 根本没导出的 API，实际从未生效过。文档站也多了独立下载页与一份可以投票的反馈路线图。

### 新增

- **优动漫 PAINT（UDM）同步副色槽、透明色板与活动槽**：UDM 和 CSP 一样，围绕色板槽的取色器状态是分开的两块，此前只能碰到主槽、没有透明色板、也没人告诉你当前激活的是哪个槽。现在第二个槽按固定步长（`+0x58`）读出，`color_index == 1` 时写入；`+0x08` 同时承载透明标志（`0xFFFFFFFF`）与激活槽索引，与 CSP 同一套布局。颜色按来源空间编码（`encode_space_values_float`）再派生其它空间，从 UDM 自己空间出发的值不再经 RGB 往返漂移。内存同步的 udm 分支转发 `source_space` / `source_values` / `transparent` / `color_index` 并对写入记账，回读不会自激；读回路径同时给出两个槽，UDM 自己切槽时发 `active_slot_changed`
- **文档站新增独立下载页与反馈系统**：下载入口从首页挪到专门的下载页（中英双语）；新增反馈页，画师不需要 GitHub 账号就能提交意见——表单经 Cloudflare Worker 代理建 GitHub Issue（Token 只存在 Worker 的服务端环境变量里，浏览器抓不到；每 IP 每分钟 1 次、正文 5~3000 字），页面内嵌一块可投票的路线图看板。反馈卡从首页抽成独立组件

### 修复

- **「焦点交还的零搅动判断」此前从未真正生效**：上一版为了不扰动笔的 WinTab 上下文，加了一条「前台与键盘焦点都还在画图软件手上时，一次系统调用都不做」的捷径，靠 `win32gui.GetGUIThreadInfo` 查焦点窗口——**pywin32 并没有导出这个 API**，于是每次都抛异常、被兜底吞掉、判定恒为「没握着」，捷径从来没走到过。现在改由 ctypes 直调 `user32.GetGUIThreadInfo`。（此前的单测替身模块恰好提供了这个函数，所以测试一路绿灯、真机却走不到）
- **取色后点画布的第一击不再被吃掉**：取色是在鼠标键**按下**那一刻确认的，而人手一次点击跨 60~120 ms，取色结束时左键通常还按着。此时交还前台/焦点，画图软件刚拿到焦点就读到「按键按下」，配对的抬起又被取色钩子吞掉——鼠标状态机就此卡住，下一次点击的按下被当成重复按下，第一击什么都不做。现在把交还推迟到 `GetAsyncKeyState` 报告两个键**物理抬起**之后（500 ms 上限）；笔的驱动不设置 `VK_LBUTTON`，所以笔的路径立即交还、不增加任何延迟
- **取色发生在自家窗口上时，交还焦点不再没有目标**：用鼠标的人常在按热键取色前刚点过 Colorink 的窗口，此时前台已经是自己，原来会直接清空「该还回去的窗口」，取色结束便无目标可还。现在保留上一次记住的目标，并新增一条兜底——找不到目标时改为记录 Z 序最顶的、可见且未最小化的画图软件窗口，且只在确实找到时才覆盖记忆，绝不抢非画图软件的前台
- **Photoshop 多一条不受前台锁限制的取回焦点通路**：`SetForegroundWindow` 受 Windows 前台锁约束，跨进程/跨完整性级别交还可能被拒。现在会在桥接目录放一个 `focus_<pid>.txt` 触发器，CEP 面板看到就派发 `com.adobe.PhotoshopLoseFocus`，让 Photoshop **自己**把输入焦点收回去——这条路径不受前台锁影响，与直接交还互为备份；桥未部署或面板过旧时静默空操作。面板协议号 14 → 15，模板里两处硬编码字面量同步更新，触发器在两条轮询分支里都先于任何颜色命令处理
- **取色钩子的欠账不再长时间挂着吃键**：「吞掉按下 ⇒ 欠一次抬起」原先用计数器记账、配 150 ms 时效，取色结束后这笔账若还没还上，全局鼠标钩子会继续挂着，最长 5 秒——这期间鼠标事件都要过钩子，用户会觉得落笔没反应。现在欠账简化成 0/1 标志，取色结束后**任何**新的按下都立刻注销欠账并卸钩，排空等待也从 5 秒压到 0.3 秒，新的一笔绝不会被旧账牵连
- **取色浮层与跟随光标的小圆点不再接受鼠标激活**：两者对 `WM_MOUSEACTIVATE` 返回 `MA_NOACTIVATE`——鼠标消息照常送到子控件，但不激活自己，避免把前台状态从正在作画的软件手里拿走（那会挂起它的 WinTab 上下文）。浮动面板的 `nativeEvent` 也改为显式返回 `(False, 0)`，与基类默认行为一致、不再经 `super()` 绕一圈
### 变更

- **`core/picker_hook.dll` 重新编译**：上一次编译误用了未优化的默认档（`.text` 比历史构建大 176 字节，段布局对不上），逻辑相同但不该就这么发出去；本次用 `/O2` 重编，与历史构建一致

## v1.8.10

「鸣谢赞助者」把支持过 Colorink 的朋友请进设置页；「第一笔丢压感」修到 v4.0——这次掐掉的是我们自己注入的合成鼠标移动、被鼠标点击带走的窗口激活、以及钩子那笔永远还不上的欠账。取色浮层不再打扰 Photoshop 的 WinTab 上下文，色轮与 Lab 的拖动也不再一路推颜色。

### 新增

- **「鸣谢赞助者」名单**：设置 → 同步页「关于」卡片新增「鸣谢赞助者」按钮，打开一份只读名单——昵称、支持月份、寄语，头像用 QPainter 现画的**首字母章**（不联网、不加载外部图片，也就不产生任何赞助者的图片请求）。名单随安装包内置（`core/data/sponsors.json`），**运行期不联网**：爱发电的 API token 一旦进了客户端就会被反编译拿到，所以拉取只在维护者侧做。对话框是**非模态**的（可以一边看一边调设置），并且会跟随设置窗口的显隐——这条是实测出来的：`Qt.Tool` 子窗口**不会**随父窗口隐藏，必须自己 mirror 宿主的 Hide/Show，同时用一个标志区分「用户主动关掉」与「被宿主连累隐藏」，否则用户关掉的对话框会在每次设置窗口显隐时自己冒出来
- **名单维护的自动化（打字量趋近 0）**：新增 `.github/workflows/sync-sponsors.yml`——一次性配好 `AFDIAN_USER_ID` / `AFDIAN_API_TOKEN` 两个 Secret，之后在 Actions 页点一下 `Sync sponsor list → Run workflow` 就完事，**0 打字 0 命令**；也可以本地跑 `tools/release/fetch_sponsors.py`。脚本**幂等**：结果没变化就不写文件、不提交，不会在 CI 里刷空提交。手工要加的条目写进 `core/data/sponsor_overrides.json`（**按昵称匹配**），同步脚本每次都会重新并入，不会被接口返回整体覆盖掉；昵称与接口返回重名时不重复出两行，只补接口给不出的字段（日期 / 头像）。日期可以随便写（`2026-8-28`、`2026-8` 都行），脚本会归一化成 `YYYY-MM-DD` 再写文件——否则每次同步都会产生一个「看着没变」的 diff 被 Action 提交

### 修复

- **Photoshop（WinTab 模式）取色后首笔丢压感 · v4.0**：v1.8.9 从钩子、窗口标志、面板回读三路修过一轮，这次又抓到四条独立的链，都指向「笔尖往画布移动的那一瞬间，谁在打扰 WinTab 上下文」：
  - **取色浮层往系统里注入的合成鼠标移动**：取色确认后 `_show_cursor()` 会调 `win32api.SetCursorPos(pos+1)` / `SetCursorPos(pos)` 去「催」前台画图软件重画笔刷光标。这两次调用是**注入的合成鼠标移动**，而且正好发生在笔尖开始往画布移动的同一瞬间——WinTab 下的 Photoshop 靠「最近的输入是笔还是鼠标」做仲裁，一次合成移动就足以让它把接下来的一笔当鼠标处理：满压感粗斑，第二笔恢复。现在不再注入（笔自己往前走，真实的移动事件会让画图软件重画光标）；要复现旧行为设 `COLORINK_PICKER_CURSOR_NUDGE=1`。注：`SetCursorPos` 是 `b84d26b`（2026-08-20）才加进去的**回归**，当时那条「`_show_cursor` 不得调用 `SetCursorPos`」的测试源码已经不在了（只剩 `.pyc`），这次连同测试一起补回来
  - **笔尖焦点交还**：新增 `core/foreground.py::StylusFocusGuard`。Photoshop 在 WinTab 模式下只把压感包送给「拥有激活 / 键盘焦点」的那个窗口的 Wintab 上下文；别的窗口一旦把激活或焦点拿走，PS 就挂起上下文（`WTEnable(FALSE)`），等用户落笔时才重新握手——握手期间到达的第一包，恰好就是带压力的那一包。取色浮层虽带 `WS_EX_NOACTIVATE`，但驱动侧的「活动窗口」判断一旦被扰动就够它挂起，所以现在在**笔尖按下之前**记住当时的前台窗口，笔抬起时把激活 + 键盘焦点还回去，让上下文在笔往画布移动的路上就恢复，而不是等落笔那一刻才恢复。三条安全边界都满足才动手：采集时前台确实是别的进程、交还时前台或键盘焦点确实落在我们手上（用户没跑去用别的软件）、目标窗口仍然存在可见且未最小化；**只挂在笔事件上，鼠标交互不碰**（免得用笔点完数值框后没法打字）。开关：`COLORINK_FOCUS_GUARD=0` 关闭、`=force` 无条件交还（诊断用）
  - **鼠标点击不再把窗口激活从画图软件手里拿走**：主窗口与浮动面板在无焦点模式下，`WM_MOUSEACTIVATE` 返回 `MA_NOACTIVATE`——不激活自己，但鼠标消息照常送到被点的子控件；避免 Windows 10 向原本处于前台的绘画软件发 `WM_NCACTIVATE(FALSE)`，那会把刚恢复的 WinTab 上下文又挂起
  - **钩子那笔永远还不上的欠账**：`core/picker_hook.c` 里「吞掉按下 ⇒ 欠一次抬起」的计数原先没有时效。驱动丢包、或按下与抬起跨了一次窗口切换时，那笔欠账会一直挂着，然后**吃掉新一笔自己的抬起**——画图软件收到只有按下、没有抬起的点击，按键就卡住了（一笔坏掉、下一笔正常）。现在给每个欠账记起始时间戳并加 150ms 时效：过期欠账遇到「取色已结束 + 新的按下」就直接注销，同时用一个 150ms 下限保证**同一次物理按下的重复 DOWN** 不会误注销那笔还得还的账
- **取色后给 PS 的「假定作画」窗口 450ms → 1500ms**：v1.8.9 开了这个窗口让 CEP 面板的周期性状态回读让路，但 450ms 比「确认取色 → 笔移到画布 → 落笔」的实际用时要短，回读可能在笔划中途恢复，把 ExtendScript 塞进 PS 主线程。现在拉长到 1500ms，并且面板侧（`photoshop_script_bridge.py` 内嵌 CEP JS 的 `Date.now() - drMtime < 1500`）与 Python 侧同步，两边不会再对不上
- **前景 / 背景的透明色没被同步**：`write_color()` 一直有 `transparent` 参数，但主窗口这条调用路径从来没传过它，于是「透明前景 / 透明背景」被当成不透明颜色推给绘画软件。现在按当前槽位取 `_fg_transparent` / `_bg_transparent` 一并传下去
- **CSP 内存模式下非 Companion 模式缺 HSV 覆盖值**：主窗口推颜色时只在 Companion 模式下从输入框凑 `hsv_u32`，其他模式一律留空。现在缺失时回落到现成的 `_get_hsv_u32_for_sync(color_index)`，色轮 / Lab 的取色在内存模式下也能带对 HSV
- **拖动色轮 / Lab 时不再一路推颜色**：`_push_color_to_sync()` 原先只识别滑杆的 `isSliderDown()`，色轮与 Lab 的拖动被当成「一次性改动」，于是拖动过程中每个鼠标移动都往绘画软件推一次颜色。现在色轮的 `dragging`、Lab 方块与 Lab 滑块的 `dragging` 都参与判定：拖动期间跳过，松手时（`on_interaction_finished`）推最终值
- **拖动色轮 / Lab 时残留系统光标**：色轮只在部分拖动分支隐藏光标，Lab 方块 / Lab 滑块与渐变色标滑杆完全没处理。现在拖动期间一律 `BlankCursor`、松手 `unsetCursor()`，笔尖压着拖动时不会再看到光标鬼影

### 变更

- **`SettingsWindow` 新增 `cfg` 属性**：设置窗口是个「外壳」，自己不持有设置，只挂共享的 `SettingsSidebar`。它的子对话框（赞助名单）按惯例用 `parent().cfg` 找实时配置——没有这个属性就静默找不到，字号之类的实时设置会跟不上
- **两份 PS 排查稿归档进仓库但不发布**：`docs/photoshop-first-stroke-plugin-audit.md`（插件争用取证与判定协议）与 `docs/photoshop-wintab-pressure-fix-plan.md`（v4.0 方案全文）现在随仓库走，但已加进 VitePress 的 `srcExclude`，**不会渲染到文档站**——里面含未在真机复现的推断与内部取证细节，不适合直接对用户公开
- **`.omc/` 加入 `.gitignore`**（与 `.omo/`、`.workbuddy/` 等本机工具状态同列）

## v1.8.9

鼠标键热键不再盖住任务栏与托盘的原生菜单，「第一笔丢压感」从取色钩子、无焦点窗口标志、Photoshop 面板回读三路合围修掉，SAI2 面板模式改用「颜色级」判据识别，本地快捷键在标签页上也能切换页签。

### 新增

- **鼠标键热键排除任务栏与托盘区域**：设置 → 快捷键新增「排除任务栏与托盘区域」（默认开启）。绑定到鼠标按键的全局热键在任务栏、托盘图标、托盘溢出面板、快速设置、通知中心、跳转列表等系统界面上不再触发，点击原样交给系统，系统原生右键菜单不会再被 Colorink 的置顶窗口盖住；键盘热键不受影响。判定由新增的 `core/input_zones.py` 负责——纯 Win32、不碰 Qt（钩子回调跑在鼠标库的处理线程上），按实屏物理坐标同时用任务栏矩形与窗口类两条判据，自动隐藏的任务栏收成 2px 边缘条以免区域消失，Win11 22H2+ 的 XAML 弹层（快速设置 / 通知中心 / 溢出菜单）只在属主是 explorer 时才算系统界面（泛用类名不会误伤别的 WinUI 弹窗）。任务栏矩形由后台线程每秒快照一次——判定路径**从不**枚举窗口，因为 `EnumWindows` / `SHAppBarMessage` 会向 explorer 发消息、在资源管理器忙碌或重启时阻塞调用线程。**全程 fail-open**：缺 pywin32、系统调用失败、任何异常都一律视为「不在系统区域」，热键照旧触发
- **兜底热键 `Ctrl+Alt+Shift+,`**：硬编码、不可解绑，任何情况下都能直接打开设置。当标题栏被隐藏、右键被占用、托盘够不着，所有可见入口都被解绑时，它是最后一条退路。它不占用户槽位，因此即使与别人的组合撞车也一定会注册（撞车时两个回调都会触发，属可接受的兜底代价）；走的是**幂等显示**而非切换，慌乱中连按不会把刚打开的设置又关掉。设置 → 快捷键页底部有一行提示标明该组合
- **托盘图标双击左键直达设置**：窗口已显示时双击托盘图标直接进入设置——一个只用左键、不依赖右键菜单的入口
- **本地视图快捷键扩展到标签页**：同一个本地快捷键（`toggleLabKey`，默认 `Space`）现在按**光标所在区域**分派：悬停在面板标签页上切到下一个页签（到最后一个回到第一个），悬停在色轮 / LAB 区域仍是切色轮 ⇄ LAB。区域内判定覆盖整个标签页矩形（标签头**和**下方当前页面），主窗口与浮窗里的标签栈都算，只有一页的栈不参与；浮窗压在主窗上时，光标真正所在的那个窗口胜出。鼠标 / 数位板笔按键会把区域判定作为前置条件——光标不在可作用区域时，该按键必须原样透传给绘画软件；按键比较先于区域命中测试，普通点击不会为一次 hit test 买单

### 修复

- **Photoshop + Wintab「第一笔丢压感」**（取色后立刻落笔时起手没有压感），三处根因一起堵住：
  - **取色钩子只吞按下、放走抬起的「孤儿抬起」**：`core/picker_hook.c` 原先吞掉确认取色的 `WM_*BUTTONDOWN`，却把配对的 `UP` 放了出去；Photoshop 收到只有抬起、没有按下的点击后鼠标 / 数位板状态机会失步，下一笔起手就丢压感。现在钩子按「欠账」计数（`pending()`），即使取色已经结束也继续吞掉那一个 `UP`，直到全部还清才摘钩（`maintenance()` 报告是否仍在挂），并新增 `uninstall_force()` 用于超时与拆除路径。`install()` 复用在位钩子而不再叠加，先前的欠账也不会被丢弃（取色在用户松手前就重新开始的情况）。配套加了一条 **30 秒硬看门狗**：万一取色流程被异常打断而没走 `stop()`，钩子会永久吞掉全系统的点击——现在无论如何都会强制收手
  - **无焦点模式不再随设置窗口开关而摘掉窗口标志**：主窗口原先在设置侧栏可见时会摘掉 `WindowDoesNotAcceptFocus` / `WA_ShowWithoutActivating` / `WS_EX_NOACTIVATE`，好让设置能正常接受焦点。设置现在是独立的顶层窗口，主窗口不需要为此让出焦点——而让出之后它会抢走 Photoshop 的前台，PS 随即关闭并重开 Wintab 上下文，下一笔就又丢压感。现在无焦点是一个全局偏好，与设置窗口是否打开无关（`update_window_flags` / `update_no_focus_policies` / `layout.py` / `settings_sidebar.py` / `picker_actions.py` 同步修正）
  - **PS 面板的周期性状态回读在写入后让路 450ms**：CEP 面板用 ExtendScript 在 PS 主线程上应用颜色，之后每 ~100ms 又一次 ExtendScript 回读状态；这些调用会阻塞 PS 的消息泵，而 Wintab 下笔的压感包队列在阻塞期间会停摆——用户偏偏就是在确认取色后立刻落笔，于是起手的压感包被丢掉。现在每次写入后由 `memory_sync` 打开一个约 450ms 的「假定正在作画」窗口（`note_color_applied` / `assume_drawing_active`），面板在这段时间里跳过周期性回读（**应用本身绝不延迟**，颜色仍即时落地）。这也是 `is_painting` 的驱动无关兜底：Huion / XP-Pen 的部分 Wintab 驱动不会为笔尖设置 `VK_LBUTTON`，只靠笔尖接触判断并不可靠
- **SAI2 面板模式检测失效（HSV 面板却按 VHSV 落位）**：旧判据比较「字段差」并硬性要求 ≤1e-4，但色槽是 SAI 内部浮点色的 **8 位取整**——仅这一项就足以让色相字段差出 0.0034，饱和度字段在近黑处更可差出 0.15，于是**任何**真实状态都过不了容差；检测返回 `None` 后代码假定 `vhsv`，SAI 按自己的 HSV 解释摆放取色点，用户看到的就是「HSV 模式却按 VHSV 坐标落位」。现在改为**反推颜色再比色槽**：每种模式把字段解释成颜色，与 SAI 自己的 8 位色槽比，误差单位变成「颜色级」（正确模式 ≈0.5 级、错误模式 ≥16 级，6000 色蒙特卡洛实测正确模式平均 0.4 级 / 最坏 0.9 级，HSV↔VHSV 中位间距 17 级），并新增 `prefer=` 保留已解析的模式以免两个同样贴切的模式来回抖动。另加**第三层信号**：SAI 自己写的六个数值读数（`read_panel_labels` / `detect_mode_from_labels`）——这些数字不会被我们的镜像写刷新，始终保留「SAI 最后一次自己设色」的颜色对，正好在色槽转成灰阶、色相无从判断时定模式（整数读数较粗，因此要求冠军比亚军领先一个余量，否则宁可不表态）
- **滑块光标锚点偏移与两端裁切**：滑块光标是按**几何中心**对准它代表的数值的，但数值轴铺满整个控件、而光标中心只在 Qt 映射值的那个内缩区间上移动，于是光标中心从来不指向它编码的那个值——在最小 / 最大处它离该值的真实位置差了半个光标；同时光标被画到紧贴控件边缘，父级裁剪矩形把朝外那一半切掉（环形主题下朝向边缘的一侧只剩一半笔画，三角主题下两端不对称）。现在统一为一个区间：`track_span()` / `_cursor_pad()` / `value_anchor_x()` 共用同一个内缩量——轨道、渐变色标、超色域遮罩与光标中心全部落在「两端各让出半个光标」的同一条轴上，槽的两端就是 `minimum()` / `maximum()`，光标中心在极值处正好落在槽端，而光标自身仍在控件内完整可见。原生 handle 宽度也改由光标墨迹范围反推（原先取主题原始宽度，正是它让中心与数值错位的根源）；行内新增左右两个随主题与 UI 缩放动态调整的补偿 spacer（`apply_slider_bleed`），槽的真实跨度不受影响。三角主题的指示条上下边缘同时对齐到整设备像素，修掉非整数缩放（105% / 110%）下指示条下方那条半亮灰边（看着像投影）
- **LAB 明度条光标位置与色域遮罩错位**：明度条光标原先用 `int()` 截断定位，每个位置至多偏上 1px；色域灰罩又按整个band 的高度算比例，与光标不在同一条轴上，导致「遮罩边界」与「光标离开色域的位置」对不上。现在 `value_axis()` / `lightness_to_y()` / `y_to_lightness()` 提供同一条内缩轴，渐变、灰罩、光标中心与鼠标取值全部共用，光标改 `round()` 并夹紧在轴内，两端的 2px 笔画都不会被 band 边缘切掉
- **标签页被非点击方式切换时残留上一页**：页面由点击以外的方式切换（本地快捷键切页签）时，`QStackedLayout` 的记账会推迟到下次布局才跑，被切走的那一页会和替换它的那一页共享同一帧。现在 `PanelHost._show_current_tab_page()` 同步只让当前页可见
- **托盘双击与右键冲突**：窗口已显示时双击托盘图标改为进入设置，未显示时才像以前一样强制显示（`follow_mouse_active` 分支保留）

### 变更

- **`toggleLabKey` 更名为「视图切换（色轮/标签页）」**：语义从「只在色轮 / LAB 区切视图」扩展为按区域分派（见「新增」），设置页标签、tooltips 与英文翻译同步更新；提示文案补上标签页那半句说明
- **新增配置键 `mouseHotkeyZoneFilter`（默认 `true`）**：旧配置缺该键时合并出默认值，开箱即受保护；改动即时生效、无需重启（勾选框变更会经 `update_hotkey_bindings` 把开关推给 `global_hotkeys` 的模块级闸门）。`pickKey` 是**唯一**对键盘路径也生效的例外——取色钩子是真的吞点击（`picker_hook.c` 直接 `return 1`），所以它必须在装钩前就被拦住，保护的是别的软件不被吞
- **SAI2 文档如实说明跟随范围**：实测（2026-09，Preview.2024.08.14）确认「镜像写」只推动色环与内部方块画面、色环标记、方块取色点、六条滑块轨道的渐变；滑块 thumb 与轨道右侧的数值读数**不会**跟随——即使强制整面板 `RedrawWindow`，字段变了、轨道渐变跟着重画，thumb 与读数仍纹丝不动（它们由 SAI 自己的控件状态驱动，只有 SAI 的输入路径能推动，而现行实现刻意不发送任何点击）。`docs/sync/sai2.md` 与 `docs/sync/sai2-color-wheel-sync-analysis.md`（新增 §10 复审）已按实测改写，并修正了旧文档里「轮区 0 px 差 ⇒ 同步成功」这个无效判据（两边颜色本来就相同，区分不了「同步成功」与「什么都没发生」）
- **FAQ 重写为「常见问题 → 热键冲突与窗口救急」**：原文是通用问答，现改为针对「右键托盘打不开菜单 / 设置进不去」的救急手册，依次给出区域过滤开关、兜底热键、托盘双击、以及手工改 / 重置配置的四级方案
- **新增 `tools/reset-colorink-hotkeys.bat` 应急重置脚本**：非破坏性——只把 `%APPDATA%\Colorink\hotkey-config.json` 改名为 `.bak`（重装软件不会重置配置，因为配置在用户数据目录里），随后以默认热键启动
- **新增 `tools/probe_tab_shortcut.py`**：在真实主窗上离屏跑「标签页区域」判定的端到端探针（16 条：标签头 / 页面内容算不算区域内、循环切页签、色轮区仍切色轮 / LAB、鼠标键与无焦点模式）
- **发布工具修正**：`version_sites.py` 补上文档站**英文**导航里的 `'Download vX.Y.Z'`（此前不在同步清单中，会在每次发版后静默漂移；`RELEASE.md` 同步注明）。契约测试由同一份清单参数化，因此自动获得这条断言
- **测试套件扩充**：新增 `tests/test_input_zones.py`（25 项：任务栏 / 托盘区域判定的半开区间与负坐标副屏、pywin32 缺失 / 调用抛错时一律 fail-open、XAML 弹层只在 explorer 拥有时算系统区域、鼠标键在系统区域不触发而在别处照常触发、开关关掉即不过滤、兜底热键是「显示」而非「切换」、每次重绑都会注册兜底热键并推送区域过滤开关），以及 `tests/test_picker_hook_symmetric.py`、`tests/test_no_focus_settings_window.py`、`tests/test_ps_drawing_window.py`、`tests/test_slider_cursor_anchor.py`、`tests/test_tab_shortcut.py`；`tests/test_sai2_ui_sync.py` 扩充模式检测（含 6000 色蒙特卡洛）。全量单测总数提升至 **1962 项（100% 全绿）**
- 版本号统一更新至 **v1.8.9 / 1.8.9.0**

### 开发说明

- 改动 `core/picker_hook.c` 后**必须重编译** `core/picker_hook.dll`（运行中的实例会以镜像方式锁住旧 DLL，重命名允许、覆盖写入会被拒），并重启 Colorink 才生效。新增的 `pending()` / `maintenance()` / `uninstall_force()` 三个导出由 `color_picker_overlay` 探测加载：**旧 DLL 仍能正常使用**，只是退回历史上「吞按下、放抬起」的行为
- 全量 `pytest` 单进程运行偶发 `Windows fatal exception: access violation`（`core/csp_companion_sync.py::connect` ← `memory_sync.run` 的 companion 分支，后台线程里 `socket.connect` 访问已释放对象）——**与本次改动无关的既有偶发崩溃**，单跑该文件必绿，分块运行亦必绿；本次的 1962 项通过数按逐文件运行汇总核对（收集数 1962 = 通过数 1962）
- 本次改动含 C 源码 + 已编译 DLL、无新增图标 / 二进制资源；`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致

---

## v1.8.8

让每一个快捷键都能解绑（设为「无」），把键位干净地交还给绘画软件。

### 新增

- **快捷键可解绑（设为「无」）**：设置 → 快捷键里每一行热键右侧新增「无」按钮，点一下即解绑；也可以点热键按钮进入录制后按 **Delete** 或 **Backspace**。解绑后该键位立刻停止响应、交还给绘画软件，热键按钮显示「未绑定」，重启后保持解绑状态，想用回来重新录制一个即可。全局取色、隐藏窗口、跟随鼠标、灰度滤镜、LAB 切换（色轮悬停 / 全局）、标题栏显示/隐藏七个热键全部支持
- **解绑是持久且彻底的状态**：空值按原样写入配置并读回，不会被默认值顶回来（`_merge_with_defaults` 不会把缺省键补成默认热键）；解绑的热键也不会进入键盘 / 鼠标全局注册，不会残留钩子

### 修复

- **三个热键的悬停提示不跟随语言**：LAB 切换（色轮悬停 / 全局）与标题栏显示/隐藏的提示原来在窗口构造时写死中文，切到 English 仍是中文；现在移入多语言提示目录，切换语言即时生效
- **Esc 与 Delete 的语义分界**：录制中按 Esc 只取消本次录制、保留原有绑定；只有 Delete / Backspace 或「无」按钮才会真正解绑，避免"想看一眼绑定却把键位清掉"

### 变更

- **设置页热键行改版**：「无」按钮只在已绑定时显示，解绑后自动收起，把宽度让给热键按钮，`Ctrl+Alt+Shift+F5` 这类长组合键的标签不再被挤断
- **文档更新**：README 与《快速上手》补充解绑的两种操作方式与生效范围
- **测试套件扩充**：新增 `tests/test_hotkey_unbind.py`（8 项，覆盖按钮状态、Delete/Backspace 解绑、Esc 只取消、`refresh_ui` 回显、真实落盘往返、不注册到操作系统），TESTING.md 的 B12 从「无自动化覆盖」升级为有自动化；全量单测总数提升至 **1692 项（100% 全绿）**
- 版本号统一更新至 **v1.8.8 / 1.8.8.0**

---

## v1.8.7

让 SAI2 的色轮跟着变色、浮窗支持多面板合并与自由拖拽，并修掉一批浮窗与面板列的高度、可见性异常。

### 新增

- **SAI2 色轮跟随**：写色后同步更新 SAI 自己的颜色面板状态，SAI 的色轮标记、滑块 thumb 与数值标签都会跟着变色；设置 → 同步新增「SAI2 面板模式」（`auto` 自动识别，也可按 SAI 里实际显示的模式手动指定 `vhsv` / `hsv` / `hsl`），改完立即生效、无需重启。同步全程只发送鼠标移动消息，不会向 SAI 注入任何点击
- **多面板浮窗（合并浮窗）**：把一块面板拖到已有浮窗上松手即并入同一浮窗，浮窗内可上下堆叠或叠成页签，浮窗与浮窗之间也能互拖
- **手动拉伸的浮窗尺寸会被记住**：手动拉过边框的浮窗会按你拉的尺寸保存并随重启还原，不再被自动缩回内容高度（从未手动拉过的浮窗仍自动贴合内容）
- **拖动抓手时的跟随缩略条**：按住抓手拖动时，鼠标下方跟着一块该面板的缩略条，落点提示画在目标窗口内，拖拽手势不再“脱离”被拖的面板
- **SAI2 连接失败原因可读**：设置页「复制诊断信息」新增 SAI2 颜色槽的解析方式与失败原因（`not_running` / `access_denied` / `no_signature` / `write_verify_failed`），未连接时能直接分辨是没开、权限不一致还是版本未适配

### 修复

- **拖入第 3 块面板时闪退**：Qt 的模态拖拽循环仍在运行时同步删除 / 重挂源抓手会释放 C++ 对象，随后拖拽几何遍历访问已释放内存，触发 Python 无法捕获的原生访问违例（窗口直接消失）。现在拖放后的挂载与重排延后到拖拽循环结束执行、抓手改为停放而非销毁，并为原生崩溃留下线程栈日志（`%APPDATA%\Colorink\faulthandler.log`）
- **抓手开关一按，浮窗就没了**：重挂面板列会让面板短暂无父并触发一次 Hide，浮窗把它当成“内容没了”而隐藏自己，面板自身标记却没变、再不会有 Show 把它叫回来。现在浮窗在重挂结束后自行重新判定可见性，多面板浮窗与页签切换都不会再被误藏
- **同一个面板出现两个家（主窗残留空抓手、列高不对）**：把主窗抓手拖到浮窗正文上时，浮窗内层宿主会把不属于它的面板就地挂进自己的树，主窗账目里还端着它。现在内层宿主只接管自己的树，跨窗口拖放统一交给浮窗层处理
- **全部面板浮出后色环变小、预览色块压到标题栏**：隐藏的滑块列仍按上一次挂载的高度计费，扣掉取色区可用高度使色环缩水；隐藏列的 `mapTo` 又报 (0,0)，把预览色块一路顶到标题栏盖住文字。现在隐藏列既不占高度，也不作为夹紧地板
- **拖走可见分组后主窗底部留灰条与空抓手**：列里只剩被关掉的分组时，仍按它们的 sizeHint 计费。现在“空列”按用户看得见的内容判定，隐藏分组的抓手随内容一起收起
- **撕出的浮窗被压扁或过窄**：刚挂载进来的面板尺寸尚未布局，47px 的面板会被撕成 22px 高的窗口、100px 宽的面板撕出 100px 宽的浮窗。现在以面板在列里的真实尺寸为准

### 变更

- **SAI 界面刷新统一为常驻全量模式**：移除设置里的刷新模式下拉与配置键（旧配置自动迁移到 schema v5），始终执行“重绘色块 + 刷新笔刷预览”，长时间作画后笔刷预览条不再滞留旧色；预览目标发现改为自愈式，失效后会自己重新发现而不是永久放弃
- **同步与重排响应更快**：单击取色不再等待 0.12s 的合并窗口（实测 154ms → 约 50ms，连续拖拽仍会合并）；把整块浮窗拖回主窗时，面板列的重挂由“每块面板一次”合并为一次
- **SAI2 版本 `auto` 名实相符**：`auto` 现在会依次尝试全部已知签名并选用能读到颜色槽的那个，全部失败才回退固定偏移；写槽后立即读回校验，不符会重试一次
- **文档更新**：README 前置在线文档站入口与徽章、新增爱发电赞助链接；浮窗文档补充合并浮窗、拖出即浮出与手动尺寸记忆说明；SAI2 指南同步改为「恒为全量刷新」的实际行为
- **测试套件扩充**：全量单测总数提升至 **1684 项（100% 全绿）**
- 版本号统一更新至 **v1.8.7 / 1.8.7.0**

---

## v1.8.6

优化 CLIP Studio Paint 手机模式连接引导、开箱默认体验与界面文案规范。

### 新增

- **CSP Companion 手机模式连接引导**：当检测到 CLIP Studio Paint 或切换至 CSP/手机同步模式且尚未配对时，自动触发一次手机配对引导弹窗，大幅降低移动端伴侣模式的配置门槛
- **开箱默认行为贴合绘画工作流**：全新安装或重置配置后，默认启用「仅绘画软件处于前台时显示」与「无焦点取色模式」，开箱即可获得沉浸式绘画体验

### 修复

- **同步状态与文案规范**：同步设置面板明确标注「CSP（手机模式）」，并在配对完成后自动刷新标题栏与同步指示灯状态
- **设置勾选框初始状态绑定**：修复设置边栏中仅在 CSP 显示、无焦点取色等复选框在首次加载配置时的默认回显一致性

### 变更

- **测试套件扩充**：新增 `test_maybe_prompt_companion_connection_prompts_when_unconnected` 与 `test_maybe_prompt_companion_connection_skips_when_session_exists` 等测试，全量单测总数提升至 **1519 项（100% 全绿）**
- 版本号统一更新至 **v1.8.6 / 1.8.6.0**

---

## v1.8.5

优化 Photoshop CEP 扩展桥双向同步响应、支持 Alt 临时吸色、升级内置面板协议至 Version 14。

### 修复

- **Photoshop CEP 扩展桥回调兼容性修复**：修复 Adobe CEP 环境下 `evalScript` 回调函数接收单一 `(result)` 参数的规范契约，彻底解决在部分 Photoshop 版本中前景色/背景色回读及多实例 PID 路由失效的偶发问题
- **同步响应灵敏度极致提升**：将 CEP 面板前景色/背景色状态回读轮询频率提升至 100ms（每秒 10 次），大幅提升 Photoshop 画布取色与调色板双向同步的实时跟手感
- **Alt 临时吸色画笔冲突规避**：绘画状态检测增加 Alt 键识别，避免按住 Alt 快速取色时误判为绘画动作阻断颜色回传

### 变更

- **内置 CEP 面板协议升级**：面板通信协议版本递增至 `Panel Version 14`，脚本执行引入 IIFE 隔离作用域与启动宽限期保护
- **测试套件扩充**：新增 `test_evalscript_callback_signature_and_state_readback` 测试，全量单测总数提升至 **1517 项（100% 全绿）**
- 版本号统一保持为 **v1.8.5 / 1.8.5.0**

---

## v1.8.4

新增 DirectComposition 硬件加速灰度滤镜后端（DComp）、滑条点击跳转与微调交互增强。

### 新增

- **全新 DirectComposition 灰度滤镜后端（DComp）**：集成基于 Windows DComp 的硬件加速灰度渲染管线（`native_dcomp/dcomp_filter.dll`），零画面撕裂、超低资源开销，提供第三种系统级无感灰度滤镜后端选择
- **滑条点击跳转与微调交互**：渐变滑条支持点击轨道任意位置直接跳转到对应数值（Jump Click），支持滑块释放信号与平滑拖拽，大幅提升多色域滑条调色效率

### 修复

- **设置外观面板与滤镜选项联动**：设置中灰度滤镜选项增加 DComp 模式，优化后端切换逻辑与状态显示
- **主窗口与取色动作细节**：微调取色器覆盖层唤醒与收起时的窗口焦点处理，提升取色响应

### 变更

- **测试套件扩充**：新增 `test_dcomp_grayscale.py` 与 `test_slider_jump_click.py` 等测试，全量单测总数由 1507 项提升至 **1516 项（100% 全绿）**
- 版本号统一更新至 **v1.8.4 / 1.8.4.0**

---

## v1.8.3

新增 PaintTool SAI 2 明度补偿色空间「VHSV」模块与色盘切片渲染、窗口尺寸绑定与高度自适应优化。

### 新增

- **全新「VHSV」明度补偿色空间模块**：精确复刻 PaintTool SAI 2 的色彩空间算法（`vhsv_to_rgb` / `rgb_to_vhsv`），带来具有独特视觉明度补偿特性的调色体验；色轮支持 `vhsv-square` 切片模式，支持在色轮中心直接拖拽调色并与色彩模型双向无损同步
- **模块切换与色轮无损往返**：色空间模块顺序扩展为 `HSV → VHSV → HLS → RGB → LCH`，模块切换时原生坐标精确保持，彻底杜绝多次中转 RGB 导致的色相漂移
- **窗口尺寸绑定与高度契约增强**：重构窗口几何约束与高度刷新机制，模块切换与面板重排时窗口平滑过渡、无多余跳动

### 修复

- **切片预热与渲染性能**：`ui/slice_prewarm.py` 与 `ui/color_wheel_rendering.py` 针对 VHSV 进行多线程预热与查表加速，拖拽与切换丝滑流畅
- **托盘与标题栏事件细节**：微调托盘右键菜单与标题栏交互，提升长时间运行稳定性

### 变更

- **测试套件扩充**：新增 `test_vhsv_integration.py` 及窗口绑定契约测试，全量单测总数由 1490 项提升至 **1507 项（100% 全绿）**
- 版本号统一更新至 **v1.8.3 / 1.8.3.0**

---

## v1.8.2

开源合规性增强（GPL-3.0 许可证与源码链接）、设置面板卡片化布局重构、取色器交互与底层钩子优化。

### 新增

- **开源合规与查看源码**：设置界面「关于与更新」卡片新增「查看源码」按钮，一键直达 GitHub 项目主页；构建脚本自动将 `LICENSE` 打包进入 onedir 便携分发目录与 zip 归档，并在 Windows 可执行文件属性及关于信息中清晰声明 GPL-3.0 授权
- **设置界面卡片化重构**：外观与同步面板重构为独立卡片布局，关于与配置管理分离卡片，数值微调与操作步骤按键优化，界面排版更清爽合理
- **取色器交互与体验增强**：取色悬浮层（`color_picker_overlay.py`）交互逻辑重构与体验优化，支持更流畅的滚轮与按键交互

### 修复

- **底层钩子与事件稳定性**：优化全局鼠标低级钩子（`picker_hook`）响应与资源释放
- **多语言与提示完善**：补充「查看源码」中英文多语言文本与控件悬停提示（Tooltips）

### 变更

- 版本号统一更新至 **v1.8.2 / 1.8.2.0**

---

## v1.8.1

新增「自动识别」同步模式、前台绘图软件识别重构、Photoshop 桥 v11 双向同步，以及切换软件时防回读。

### 新增

- **自动识别同步模式**：`syncSoftware` 默认值由 `csp` 改为 `auto`——启动和运行中根据**当前前台绘图软件**自动选择同步模式（CSP / SAI2 / UDM / Photoshop / 手机 Companion）；同步页在 auto 下指示当前激活的后端，切换前台软件时无需手动改设置
- **前台绘图软件识别重构**：`core/foreground.py` 把绘图应用按模式映射（`photoshop→ps`、`sai→sai`、`csp→csp`、`udm→udm`），并新增**非绘图应用排除列表**（浏览器 / 系统外壳 / 开发工具 / 编辑器 / 办公通信 / 媒体娱乐进程 + 窗口类 + 网页标题关键词），前台匹配更准确、不再被浏览器 / 编辑器误判
- **Photoshop 桥 v11**：`PANEL_VERSION` 9 → 11，命令支持 `both`（一次同时写前景 + 背景），并新增 `client_alive.txt` / `drawing.txt` 标记；配合面板状态回读节流（0.5s → 1.0s）与每 40 tick 心跳

### 修复

- **切换软件防回读**：`set_software_mode(mode, initial_palette)` 支持一次性 seed 调色板——切到新软件（如从别的软件切回 Photoshop）时立即以当前调色板播种写入追踪并抑制过期回读，避免把颜色切回去
- **切走清理**：离开 Photoshop 模式时清理运行时标志（`cleanup_runtime_flags`），避免残留状态影响下次连接
- **前台识别防误判**：明确排除浏览器 / 编辑器 / 办公等非绘图应用，`onlyShowInCsp` 与自动同步切换不再被网页 / 代码窗口触发
- **UI 调整**：色环隐藏模式下切换形状 / 布局不再误触发 `_refit_preview_box`；主题、面板浮动、同步页提示、preview box 等多项细调

### 变更

- **`syncSoftware` 默认值 `auto`**：旧配置仍保留显式选择（csp / sai / udm / ps / companion），仅新安装默认自动识别；`MemorySyncThread.set_software_mode` 新增可选 `initial_palette` 参数，向后兼容
- **Photoshop 桥协议升级到 v11**：面板版本号更新，旧部署面板会提示重启 Photoshop

### 开发说明

- 本次无新增图标 / 二进制资源，均为纯 Python / UI 逻辑；`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致
- 新增 / 扩展测试：`test_foreground_detection.py`（前台识别排除列表）、`test_photoshop_script_bridge.py`、`test_memory_sync_ps_echo.py`、`test_sync_mixin.py`、`test_companion_protocol.py`、`test_ringless_lifecycle.py` 等，测试总数 1450 → 1485（+35）
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.8.1 / 1.8.1.0**

---

## v1.8.0

前台标签页防跳页、全局取色画笔同步与极速低延迟、Float RGB 高精度色彩桥梁、滑条 Qt 类型安全隔离、有效色域范围动态跟随与双向完全同步，并包含面板窗口布局重构与 LAB 圆形色盘增强。

### 新增

- **前台标签页防跳页**：调整选项、切换色彩模式或刷新设置时，尊重用户当前停留的前台标签页，不再强行切走（`ui/panels/host.py` 记录活动页并在重建后恢复）
- **全局取色画笔同步 & 极速低延迟**：Companion 模式同步由 450ms 套接字超时等待改为非阻塞 drain，延迟降至约 5.5ms~7.5ms（瞬时落笔）；全局取色后完整保留原始取色数据并立即 flush 写入 CSP，修复取色后画在 CSP 上颜色不一致的问题
- **面板窗口布局重构**：新增可拖拽 / 浮动 / 合并重排的面板系统——页签拖拽悬停自动切换、页签页空白区追加、同页合并重排、顶部间距撑高底部边距联动、页签主题化与表头重排 / 合并、浮动面板拖回页签头不再崩溃、单面板页签内容顶部对齐等；新增「顶部以及底部距离」设置
- **LAB 圆形色盘增强**：圆形模式下点击副点 / 主点防抖容差、锚点反解跟随、矩形 90° 方阵、LAB 色盘纯几何圆形遮罩、和谐点双侧可点击

### 修复

- **有效色域范围动态跟随 & 双向完全同步**：① 色域内微调明度时色度恒定、有效范围零抖动；② 移出有效范围时自动沿边界映射并动态扩展有效色域，手柄始终包络在有效区间内；③ 底部滑条（OKLab / LAB / OKLCh）与右侧纵向滑条算法与数据打通，双向实时同步
- **滑条色彩恢复与 Qt 类型安全隔离**：数学计算层使用高精度浮点，委托给 PyQt6 QColor 与控件绘图前显式安全取整，杜绝 C++ 底层 TypeError；所有滑条彩虹渐变与彩色刻度正常渲染
- **面板 / 窗口细节修复**：swatch 簇在握把切换后重新适配而非挤压、容器几何变化时重新钳制、所有放置路径钳制到滑块区之上；页签页在首帧绘制前隐藏、多个启用分组堆叠、页签标签不截断等
- **其他修复**：源运行使用自己的单实例锁；模拟滑动 / 拖拽回归测试更新

### 变更

- **Float RGB 高精度数学桥梁**：中转色彩桥梁由 8-bit 整数升级为未截断的高精度浮点 RGB（误差降至约 1.05e-9，精度提升超过一百万倍），彻底消除因整数离散截断导致的反算噪点；`Color` 新增 `rgb_float` 字段与 `r_float/g_float/b_float` 访问器，供高精度计算链使用
- **全局取色采样**：`ColorPickerOverlay` 新增 `_sample_pos` 精确采样点击位置的原始像素（而非仅取中心色），并保留完整 HSV 数据
- **测试基线**：新增 `tests/test_picker_color_sync.py`（全局取色同步 / 唤醒冲刷 / Float RGB / 色域跟随），与既有的面板 / 窗口 / 主题 / 颜色会话等大量新增测试；其余全量测试在排除后台线程 socket 崩溃用例后通过

### 开发说明

- 本次包含较大规模面板 / 窗口布局与主题重构（新增 `ui/panels/*` 目录、`ui/window/floating_mixin.py`、`ui/window/panels_mixin.py`、`ui/window_layout.py`、`ui/border_themes.py`、`ui/chrome_opacity.py`、`ui/color_session.py`、`ui/preview_clearance.py`、`ui/theme_contrast.py` 等），均为纯 Python 静态导入，无新增图标 / 二进制资源；`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致
- **注意**：全量 pytest 在 `test_picker_color_sync` 相关用例下因后台 QThread 访问已释放 socket 触发 Windows access violation（v1.7.1 基线 1450 passed 无崩溃，工作区功能改动后出现）；本版按约定未修复该测试崩溃，发布以排除该后台线程 socket 崩溃用例后的全量回归为准
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.8.0 / 1.8.0.0**

---

## v1.7.1

设置界面悬停提示全面补全（tooltip 目录化 + 语言切换重应用），开机自启动改为当前用户免管理员模式，并修复自启动状态持久化。

### 新增

- **设置页悬停提示**：新增 `ui/settings/tooltips.py` 集中维护设置控件悬停提示（中文源串 + 统一走 `i18n.tr`），覆盖热键 / 界面 / 取色器 / 滑块 / 同步 / 更新等 46+ 项控件；`apply_settings_tooltips()` 在每次界面（重）构建后调用，语言切换 / `retranslate()` 后提示保持中文 / 英文同步
- **英文翻译**：`core/i18n.py` 为全部新增提示补英文目录，英文模式下不会静默回退中文
- **设置窗口关闭按钮提示**：`_TitleBar` 关闭按钮补充「关闭设置窗口」悬停提示

### 修复

- **自启动状态持久化**：设置侧栏「开机自启动」此前注册表写入成功后没有把新状态写回 `self.cfg`，下次 `refresh_ui()` 会重新加载旧值，勾选看起来"保存不生效"，取消勾选也无法清掉已注册的 Run 键；现在注册表写入成功后才落配置（失败仍回滚），配置与勾选一致时保持幂等同步
- **可移植版自启动失效自愈**：单文件 EXE 被重新下载 / 移动后，Windows 自启动入口仍指向旧路径，「开机自启动」静默失效；`main.py` 启动时重新断言 HKCU Run 条目，使其始终与持久化的 `openAtLogin` 设置一致（`apply_autostart` 幂等，非冻结 Windows 构建下为 no-op）

### 变更

- **开机自启动模式**：从「以管理员权限启动（免 UAC 弹窗）」改为「开机后自动启动 Colorink（仅当前用户，不需要管理员权限）」——走当前用户 HKCU Run 注册表，无需管理员授权；界面提示文案同步更新

### 开发说明

- 新增 `ui/settings/tooltips.py`（纯静态目录 + 重应用函数），无新增图标 / 二进制资源；`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致
- 新增回归测试 `tests/test_autostart_setting.py`（自启动持久化）与 `tests/test_settings_tooltips.py`（tooltip 完整性 + 英文翻译覆盖），测试总数 832 → 840（+8）
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.7.1 / 1.7.1.0**

---

## v1.7.0

LAB 面板新增环形色盘（circulant LAB disc）与和谐模式，配套设置开关与一系列对齐 / 光标 / 渲染修复，并清理误入库的调试文件。

### 新增

- **环状 LAB 色盘**：LAB / OKLab 视图新增圆形色盘渲染，与色环（hue ring）同心、同半径；按当前色相的色域边界平滑绘制，鼠标拖出即吸附到最近可见边界（复用 v1.6.17 的边界吸附语义），前景 / 背景独立可调
- **五种和谐模式**：互补、补色分割、类似色、三角、矩形（rectangle）——以基准色为中心的色相角偏移（OKLab / CIELAB 度），在色盘上以指示点标注，点击即取色；基色始终含 0° 位
- **设置开关**：设置页新增 LAB 形状（圆形 / 方形）与和谐按钮显示开关，对应配置项持久化；面板按开关自适应布局
- **后台预渲染**：新增 `ui/lab_prewarm.py`（QRunnable 线程预计算环形色盘位图）与 `ui/lab_harmony.py`（纯和谐数学），色盘绘制不在 GUI 线程卡顿
- **回归测试**：新增 `tests/test_lab_disc.py`（243 行）+ 扩展 `test_ringless_panes.py` / `test_transparent_swatch.py` / `test_ringless_lifecycle.py`，测试总数 801 → 832（+31）

### 修复

- **LAB 色盘蓝色接缝**：sRGB 色域恒定亮度 OKLab 截面轻微非凸，蓝色方向色域边界在极小角度内跳变约 0.03 C，形成刀切状径向接缝；现按 ±10° 圆形滑动平均作为径向标尺并按原始边界钳制，描边处处连续且像素不越界
- **LAB 色盘尺寸与对齐**：色盘不再因预览条收缩而变小，精确对齐色环圆心与半径；预渲染与交互映射共用同一平滑边界计算
- **LAB 视图光标**：LAB 色方块 / 圆盘补上十字光标（与色轮一致），取色拖拽期间按空格隐藏、松开恢复，与色轮行为统一；笔悬停也随光标形状同步
- **左上预览透明贴片**：`previewBoxPosition=top-left` 时透明胶囊移到前景 / 背景圆之上（与左下放置镜像），圆形几何、命中测试与鼠标掩码共用同一偏移，点击与绘制对齐
- **浮动切换簇**：与边缘锚定、共享主题统一，不随窗口缩放漂移

### 变更

- **配置模式**：新增 LAB 形状 / 和谐按钮配置键；旧配置缺省兼容（默认圆盘 + 和谐按钮显示）
- **清理误入库文件**：`3a8502e` 曾把 `_v7_panel.js`、`qrcode_*.jpg`、`docs/superpowers/plans/*` 等调试 / 草稿文件卷入仓库，本版移除并在 `.gitignore` 追加对应模式，避免后续误提交

### 开发说明

- 本次无规格 / 资源变更：`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致，构建自检通过；新模块为纯 Python 静态导入
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.7.0 / 1.7.0.0**

---

## v1.6.18

Photoshop 同步设置界面文案与实例选择简化：统一实例标签、目标下拉固定为 auto。

### 变更

- **Photoshop 实例标签统一**：检测到的实例下拉统一显示 `<文件夹> (同步桥)`，去掉误导性的 `(COM)` / `(绿色版·脚本桥)` 后缀——v1.6.17 起所有版本共用同一条 CEP 桥路径，标签不再暗示不同后端；`kind` / `progid` 仍作为内部记录保留
- **「移除扩展」文案更新**：不再提示「绿色版 Photoshop 专属」，改为通用「删除已部署的 Photoshop 同步扩展」，与统一桥的现状一致
- **Photoshop 目标下拉固定为 auto**：所有版本共享同一 CEP 桥路径并按 PID 路由，列出具体实例不再提供有意义的选项；下拉现在始终为 `auto`（自动选择第一个运行中的实例），避免误导

### 开发说明

- 纯 UI 文案与下拉行为调整，无逻辑/资源变更；配套更新 `tests/test_photoshop_instances.py`、`tests/test_photoshop_color_sync.py`
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.6.18 / 1.6.18.0**

---

## v1.6.17

Photoshop 桥接重写为单一用户级 CEP 扩展（修复绿色版 / 多实例同步恶化并大幅降低引擎占用）、取色器同步线程暂停、色盘 / 色轮边界吸附修复与颜色历史去重升级。

### 新增

- **单一用户级 CEP 桥**：一个隐藏的 CEP 扩展（`%APPDATA%\Adobe\CEP\extensions`）即可服务所有 Photoshop 版本（正版 / 绿色版 / COM 破解版），不再逐安装部署、无需管理员权限，同步面板新增「移除扩展」按钮一键清理
- **多实例按 PID 路由**：`cmd.txt` 携带目标进程 ID，每个面板只应用发给自己的命令；换新 Photoshop 后陈旧命令按 mtime 认领，不会误吃新命令
- **取色器性能**：取色遮罩激活时暂停同步线程，拖拽采样期间不再抢 GIL，取色后恢复并写入颜色

### 修复

- **Photoshop 颜色不同步（绿色版）**：绿色版 ExtendScript 拒绝 `new SolidColor()`（`EvalScript error`），改为原地 mutate `app.foregroundColor` / `app.backgroundColor`，色槽同步恢复
- **Photoshop 双向同步失效（`$.pid` 不可用）**：绿色版返回 undefined 导致 state/heartbeat 永远匹配不上，现在先经 CEP 宿主环境（appPid）取 PID，再回退 `$.pid`，仍不可用时降级为共享文件并应用所有命令，`mypid.txt` 记录当前模式
- **CEP 桥饿死脚本引擎**：面板轮询由 100ms 全写改为 1s tick + 事件驱动回读 + 4s 心跳，TourBox / Coolorus 等 CEP 插件不再卡顿；连接时清理其他实例遗留的旧版桥，避免固定扩展 ID 争抢 CEP 运行时
- **色盘 / 色轮边界吸附**：拖拽到 RGB / OKLCh / LAB / OKLab 色域外时落在**离鼠标最近的边界点**，而不是被钳到矩形角落或沿射线拉回中心；原先越界拖拽会粘在方框角上
- **颜色历史去重**：已存在于历史的颜色被提升到第一格而不再重复追加，只有真正的新颜色才入列（超出容量仍按 FIFO 丢弃最旧）

### 变更

- **Photoshop 回读节流**：COM 后端回读节流至 0.5s（避免每 100ms 触发 6~8 次真实 COM 往返把 PS 引擎占满）；脚本桥后端取消双重节流，只依面板自身写 cadence，PS→Colorink 延迟从 ~1s 降到 ~0.5s
- **快速命令路径**：面板命令邮箱每 100ms 经 Node fs `statSync` 检查（零 ExtendScript 空闲流量）；状态回读 1s + 命令后立即回读，心跳 4s；无 Node 的 CEP 构建回退轻量 evalScript 探测
- **测试基线**：新增 `tests/test_slice_boundary_snap.py`（RGB / OKLCh / LAB / OKLab 边界吸附 + 精确映射），测试总数 779 → 801

### 开发说明

- 本次无规格 / 资源变更：`Colorink.spec` 与 `Colorink Onefile.spec` 资源集保持一致，构建自检通过
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号，统一到 **1.6.17 / 1.6.17.0**

---

## v1.6.16

默认热键改为 Ctrl+Alt 组合避免与绘画软件冲突、数位板笔悬停光标修复、移除 CSP 5.1 内存同步模式并在模式选择界面标注各模式问题、推荐使用手机模式。

### 新增

- **默认热键改为 Ctrl+Alt 组合**：首次启动默认全局热键由 `F11` / `Ctrl+R` / `Ctrl+H` / `Ctrl+Shift+T` / `Ctrl+G` / `Ctrl+L` 改为 `Ctrl+Alt+Q` / `Ctrl+Alt+J` / `Ctrl+Alt+Y` / `Ctrl+Alt+K` / `Ctrl+Alt+D` / `Ctrl+Alt+L`，大幅降低与 CLIP STUDIO PAINT / Photoshop / SAI 默认快捷键的冲突概率；配置、设置界面回退值、测试、README 与手动清单同步更新
- **真机笔悬停诊断工具**：新增 `tools/probe_tablet_hover.py`，实时打印笔 / 鼠标事件的类型、来源、指针类型、光标位置与强制结果，便于在真实数位板上核对驱动行为（可通过 `COLORINK_DEBUG_PEN=1` 输出运行时诊断）

### 修复

- **数位板笔悬停光标不变化**：Windows 下笔的悬停移动以 `QTabletEvent(TabletMove)` 到达应用，Qt 不会为这类事件重跑窗口部件光标逻辑，色轮上的十字光标只在鼠标悬停时出现；现在笔悬停时按笔下方窗口部件强制同步系统光标（十字 / 手型 / 缩放箭头 / 拖拽隐藏），与鼠标行为一致，Windows Ink 与 Wintab 驱动、各品牌数位板均适用
- **回归测试**：新增 `tests/test_tablet_cursor.py`，覆盖笔悬停色轮十字、窗口边缘缩放光标、拖拽隐藏光标、离开窗口还原箭头与锁定窗口尺寸分支

### 变更

- **移除 CSP 5.1 内存同步**：同步页「CSP 版本」下拉不再提供 5.1 选项（含前景 / 背景 / 透明同步能力说明），旧配置 `cspVersion=csp5.1` 自动迁移回 `auto`
- **内存模式遇 CSP 5.1 拒绝连接**：自动检测到 CSP 5.1 时不再附接 5.1 内存布局，同步页提示「CSP 5.1 内存同步已移除，请改用手机（Companion）模式」，避免 5.1 副本扫描/槽位错位等内存同步历史问题（v1.6.9 / v1.6.13 / v1.6.14 系列缺陷）
- **模式选择界面标注各 CSP 模式问题**：CSP 内存同步标注「仅 4.x/5.0 主色、依赖内存扫描、易随 CSP 更新失效」；`CSP Companion（手机）` 标注「推荐」，并新增一行提示说明手机模式连接稳定、支持前景 / 背景与透明

### 开发说明

- 保留了 `csp5.1` 后端 profile 定义与诊断工具（不被主动附接），仅移除 UI 选项并在连接层拒绝，方便排查与回归
- 已同步更新 `core/updater.py`、`file_version_info.txt`、`release_notes.md` 三处版本号

---

## v1.6.15

数位板笔悬停光标与色轮跟随修复。

### 修复

- **数位板笔悬停光标不变化**：Windows 下笔的悬停移动以 `QTabletEvent(TabletMove)` 到达应用，Qt 不会为这类事件重跑窗口部件光标逻辑，色轮上的十字光标只在鼠标悬停时出现；现在笔悬停时按笔下方窗口部件强制同步系统光标（十字 / 手型 / 缩放箭头 / 拖拽隐藏），与鼠标行为一致，Windows Ink 与 Wintab 驱动、各品牌数位板均适用
- **回归测试**：新增 `tests/test_tablet_cursor.py`，覆盖笔悬停色轮十字、窗口边缘缩放光标、拖拽隐藏光标、离开窗口还原箭头与锁定窗口尺寸分支
- **真机诊断**：新增 `tools/probe_tablet_hover.py`，实时打印笔 / 鼠标事件的类型、来源、指针类型、光标位置与强制结果，便于在真实数位板上核对驱动行为

---

## v1.6.14

CSP 内存同步前景 / 背景对称恢复与诊断增强发布。

### 新增

- **前后景槽对称恢复**：主色槽（前景）与副色槽（背景）的副本集现在都可以跨 `_SLOT_STRIDE` 从另一槽的副本集推导，方向对称；任何一次成功定位都会免费记住另一槽的兄弟副本，后续平凡色（黑 / 白）写入不必再全地址空间扫描
- **降级状态记忆与自愈**：写入首次退化为仅 UI 镜像时，记录权威副本仍持有的旧模式；下一次定位凭此识别真实副本并写回 CSP 笔刷，槽位不再整会话锁死在镜像上
- **复制定位诊断**：新增 `copy_locate_state`，记录每个色槽最近一次定位结果（`located` / `cached` / `derived` / `mirror-only`），并随设置页「复制诊断信息」输出，一眼判断“颜色不同步”是否只写到了镜像
- **回归测试**：新增 6 个测试覆盖前景镜像 bug 的对称路径、污染镜像自愈、无陈旧模式时的多副本一致恢复、错误推导拒绝与诊断状态上报

### 修复

- **CSP 前景色不同步、背景正常**：v1.6.13 上主色槽定位失败一次后就永久退化为 UI 镜像——镜像随我们自己的写入移动，之后的扫描永远无法再匹配权威副本；现在主色槽与副色槽享有对称恢复路径，前景重新跟到 CSP 笔刷
- **平凡色首笔写入**：CSP 默认黑前景 / 白背景（12 字节全 0，或 8 个 0 + 4 个 0xFF）在进程内存里到处命中时，只要另一槽可定位，写入仍能跨槽推导抵达权威副本，而不是只写镜像

### 变更

- **诊断信息**：`core/diagnostics.py` 后端属性增加 `copy_locate_state`，CSP 同步状态从只报进程 / 版本扩展为可判断写入是否到达笔刷
- **测试基线**：`TESTING.md` B8 覆盖清单加入 `test_csp_copy_locate.py` 与 `test_csp_legacy_profiles.py`，测试总数 755 → 761
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.14` / `1.6.14.0`

---

## v1.6.13

七项缺陷修复、灰度 / CSP 稳定性与性能优化发布。

### 修复

- **预览命中区域与绘制不一致**：无 Ringless 的旧式前景 / 背景圆盘命中测试此前使用过期的尺寸常量（前景盘实际大出约 13% 且偏移数像素），现在绘制、命中与遮罩共用同一几何
- **OKLCh 色环色相解析**：修正色相分辨率相关缺陷，色环点击 / 拖拽得到的目标色相与显示一致
- **Companion 透明与 HLS**：透明清除标志始终随颜色发送，不再被合并路径吞掉；HLS 纯黑不再被误判为空数据丢弃
- **Companion 断线判定**：收到 EOF 现在明确视为断线并走重连流程，不再让同步线程悬在已断开的会话上
- **更新下载完整性**：按解析后的总字节数校验下载完整性，老资产无 size 字段时也会用 Content-Length 拦截截断的包
- **配置迁移 v3**：移除只写不读的 `labVisualizerMaxVal`，旧配置自动清理，LAB/OKLab 轴上限保持由渲染模式决定
- **灰度滤镜生命周期**：`close()` 现在真正停掉屏幕监听与故障看门狗；预热失败后重试捕获而不是复读上次错误
- **CSP 首笔同步**：会话内第一次背景色切换现在真正写进 CSP 笔刷；副本地址缓存被预热，第一笔就用新颜色；connect 不再主动全内存扫描（恢复按需定位）

### 性能

- **灰度运行时降噪**：停止每 3 秒转储一次全部线程栈，减少轮询线程的无效开销
- **CSP 副本缓存预热**：写入路径首笔命中不再退化为只写镜像槽

### 变更

- **开源协议**：`LICENSE` 由 MIT 更换为 **GPL-3.0**（copyleft），使用、修改或分发本代码时衍生作品必须同样以 GPL-3.0 开源；README 同步更新
- **支持作者**：README 新增「请我喝杯咖啡 ☕」区块与赞赏二维码（`screenshots/buy-me-a-coffee.jpg`）
- 新增 `tests/test_bugfix_regressions.py` 等回归测试与 `tools/diag_sub_copy_stride.py` 诊断脚本，测试总数 706 → 755
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.13` / `1.6.13.0`

---

## v1.6.12

SAI 界面刷新与无焦点取色加固发布。

### 新增

- **SAI 界面刷新**：内存写入颜色后，SAI 自己的画笔色块会跟着重绘，不再停留在旧颜色。三档可选：`repaint`（默认，纯重绘请求、不注入任何输入）、`full`（额外刷新缓存位图的笔刷预览，需连发三下点击走完整背景循环，副作用自担）、`off`（旧行为）；设置页同步页签内新增下拉框与悬停说明
- **配置 schema v2 迁移**：早期把 `saiUiRefresh` 默认成 `full` 的用户自动迁移回 `repaint`，避免 SAI 记住合成点击的按下点、下一笔出现楔形起笔；想用 `full` 可重新手动开启
- **SAI 探针工具**：`tools/probe_sai_ui_refresh.py` / `tools/probe_sai_windows.py`，测量 SAI 控件渲染与刷新方式，复现并定位界面不同步问题

### 修复

- **无焦点取色仍抢焦点**：取色放大镜与十字光标补 `WindowDoesNotAcceptFocus`、`WA_ShowWithoutActivating` 并直接加 `WS_EX_NOACTIVATE` 扩展样式，show 前后双重应用；设置窗口打开/关闭时同步主窗口 flags，避免设置面板无法正常操作
- **取色结束绘画软件笔刷光标不恢复**：恢复系统光标后微移再复位鼠标位置，促使前台绘画软件重新应用笔刷光标，无需额外点击
- **用户手动隐藏被立刻拉回**：热键隐藏统一走 `toggle_visibility`，托盘单击隐藏、双击显示、关闭到托盘都维护 `_user_hidden`；前台追踪器在用户手动隐藏期间不再自动重新显示窗口
- **无焦点模式开关不彻底**：禁用 no-focus 时现在会清除 `WS_EX_NOACTIVATE`，真正恢复正常激活行为；程序化刷新 flags 时用 `WA_ShowWithoutActivating` 防止抢焦点

### 变更

- 新增 SAI UI 刷新相关单测（模式归一化、安全护栏、设置联动、SAI2 写入钩子），测试总数 642 → 706
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.12` / `1.6.12.0`

---

## v1.6.11

原生灰度滤镜相机启动修复发布。

### 修复

- **灰度滤镜切换后无画面 / 无法激活**：GUI 线程调度桥 `_dispatch_invoke` 原先调用 `getattr(widget, name)(widget)`，而实例方法已是绑定方法，多传的 `widget` 触发 TypeError；异常又被 except 静默吞掉，导致相机启动 kick 从未执行、帧循环永远不开始。现在改为 `getattr(widget, name)()`，kick 真正执行，覆盖层能收到第一帧

### 新增

- **调度桥回归测试**：`test_dispatch_invoke_calls_bound_method_without_extra_arg`，锁定绑定方法不得再传 widget 的调用约定

### 变更

- 测试总数 641 → 642
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.11` / `1.6.11.0`

---

## v1.6.10

前台检测与绘画软件识别修复发布。

### 修复

- **启动即误显示**：开启「仅在绘画软件前台时显示」且无绘画软件在前台时，前台跟踪器先判定窗口应保持隐藏，`main.py` 不再用无条件 `show()` 覆盖该决定
- **前台会话被空缓存钉死**：进程 exe 解析失败（进程还在启动 / 杀毒拦截 / 受保护进程）不再写入空缓存，下一 tick 会重试，避免整个前台会话都无法识别绘画软件
- **大小写敏感误判**：`_exe_matches_drawing_app` 与 `_title_matches_drawing_app` 内部先转小写，Windows 返回原始大小写的进程名（`CLIPStudioPaint.exe`）与窗口标题（`CLIP STUDIO PAINT`）不再漏判
- **窗口已隐藏时不记录限制状态**：非绘画软件前台且窗口本就隐藏时也写入 `auto_hidden`，与启动判定保持一致

### 新增

- **遗留 CSP profile 回归测试**：新增 `tests/test_csp_legacy_profiles.py`，覆盖 CSP 4.x / 5.0 的 `u16x2_dup` 槽布局读写路径，并保证遗留路径绝不触碰 5.1 的副本定位机制
- **手测清单**：新增 `CHECKLIST.md`（纯手动打勾项，自动化命令已剔除），README 与 TESTING.md 增加入口链接

### 变更

- 前台检测测试增加大小写 / 瞬时失败 / 启动隐藏场景覆盖
- 测试总数 617 → 641
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.10` / `1.6.10.0`

---

## v1.6.9

CSP 内存同步稳定性修复发布。

### 修复

- **纯黑颜色同步卡死**：CSP 5.1 内存同步的副本定位扫描对全 0 模式（纯黑 = 12 个零字节）无上限收集命中，每次写入都会全地址空间扫描并产生上亿命中，轮询线程被堵死、内存同步整体失效；现在扫描带命中上限，平凡模式（黑 / 白 / 灰）不再逐个收集
- **降级写死与重复全扫**：定位失败后不再每次写入都全量重扫（扫描带冷却），降级结果不缓存；纯黑等平凡模式回退到最后一次已验证的副本集，让纯黑也能真正写进笔刷而非只更新 UI 镜像
- **扫描窗口跨页漏匹配**：连续读取窗口按模式长度重叠，修复恰好跨窗口边界的颜色副本扫描不到
- **主 / 副色同色互相覆盖**：CSP 5.1 主色与副色结构相距 `0x60`，两槽同色时按值扫描会把兄弟槽也命中，写主色会顺带覆盖副色（实测副色被写成主色）；现在过滤兄弟槽命中
- **重启 CSP 后写错地址**：断开连接时清空副本地址缓存与已验证副本集，CSP 重启获得新地址空间后不再写进新进程的无关内存
- **设置应用误断连接**：`set_version("auto")` 不再把自动检测当成显式 profile 处理，避免每次应用设置都把已连接的 CSP 5.1 切成 4.x、断开进程句柄并清空缓存
- **历史颜色来源错位**：外部同步改色时先记录 RGB 为活动源，修复历史格用上一次交互的坐标空间记录新颜色

### 新增

- **副本定位回归测试**：新增 `tests/test_csp_copy_locate.py`，覆盖命中爆炸、扫描冷却、记忆副本集、兄弟槽过滤、降级状态恢复等场景
- **测试清单文档**：新增 `TESTING.md`，按改动区域给出自动化与手测档位；README 增加入口链接
- **调试开关**：`COLORINK_CSP_DEBUG=1` 环境变量即可打开内存同步跟踪日志，排查不同步问题无需改代码

### 变更

- 副本扫描命中上限为 `_SUB_COPY_LIMIT + 1`，窗口读取重叠 `len(pattern) - 1` 字节
- CSP 调试日志改由环境变量控制，默认关闭
- 测试总数 598 → 617
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.9` / `1.6.9.0`

---

## v1.6.8

设置页重组与代码模块化重构发布。

### 新增

- **设置页重组**：导航由 5 页调整为 6 页（快捷键 / 界面 / 取色器 / 滤镜 / 同步 / 关于），灰度滤镜设置独立为「滤镜」页，绘画软件同步独立为「同步」页；界面语言切换移入「界面」页
- **onedir ZIP 更新支持**：更新检查可识别 `Colorink-Onedir.zip` 便携目录资产，onedir 安装不再误下 GitHub 源码 zip，找不到 onedir zip 时自动回退 EXE
- **界面文案补全**：补齐 Ringless、滤镜、同步、无焦点取色等场景的中英文翻译，减少英文界面下未翻译回退

### 修复

- **取色历史框线**：悬停 / 选中框改为独立覆盖层绘制，修复高分屏下历史格接缝处蓝框断裂、错位与残留
- **历史格选中同步**：颜色历史写入统一色彩模型时补 `QColor` 包装，修复选中态在部分场景下不刷新
- **设置重组回归**：同步与版本信息同卡、语言组合框归属、滤镜后端选项等随页面拆分同步修正，全部原设置项保留

### 变更

- **模块化重构**：`core/csp_brush_link.py` 拆为 `core/csp_brush_link` 包；`ui/color_wheel.py` 拆为 geometry / interaction / prewarm / rendering；`ui/main_window.py` 拆为 `ui.window` 关注点 mixin；`ui/settings_sidebar.py` 拆为 `ui.settings` 面板；对外导入路径保持兼容
- **文案统一**：诊断信息中的「同步软件」改为「同步目标」；「黑白滤镜」「无焦点选色模式」等文案统一为「灰度滤镜」「无焦点取色模式」
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.8` / `1.6.8.0`

---

## v1.6.7

更新检查加固与灰度滤镜可靠性修复发布。

### 新增

- **更新检查加固**：安装包下载改为先落 `.part` 临时文件，按字节数 + SHA-256 校验通过后原子改名；支持可选 `COLORINK_GITHUB_TOKEN` 请求头，403 / 404 / 网络错误分别携带 `error_detail` 结构化报错
- **双形态安装包识别**：自动区分 onedir / onefile 两种构建形态并选择对应资产（onefile 取体积最大的 EXE），自替换前额外校验 EXE 所在目录可写
- **跳过版本**：手动与托盘更新检查共用同一流程，可记录并跳过指定版本；托盘菜单与设置页均接入应用内下载流程
- **灰度运行时纳入仓库**：`native_grayscale/runtime/grayscale_overlay.pyc` 随仓库分发，`build_runtime.py` 可复现编译；原生灰度不可用时自动回退 Mag 后端

### 修复

- **灰度滤镜无法激活**：运行时用 GUI 线程信号桥替换从未注册进元对象的后注入 `pyqtSlot`，相机 kick 真正触发；屏幕布局 / DPI 变化时重新应用覆盖层几何；健康检查杀掉实现后不再永久卡在 `_warming`
- **托盘崩溃与空转**：持有上下文 QMenu 引用（原被 GC 导致右键悬空指针）；修复单击 / 双击重复切换；启动更新检查结果改由排队信号投递到 GUI 线程；窗口隐藏期间不再武装 0ms 高度定时器（原会空转占满单核）
- **同步断连不恢复**：CSP 5.1 黑白降级副本不再被缓存（镜像降级态不会永久锁定）；CSP / SAI2 / UDM 连续读失败计数清零后丢弃过期连接，软件重启后自动重连
- **64 位句柄截断**：PS 同步、前台检测、SAI2 等 ctypes 调用补齐 `restype` / `argtypes`（`OpenProcess` / `WaitForSingleObject` / `CloseHandle`）
- **热键与配置健壮性**：捕获不支持按键不再清空已绑定热键，重绑先注册新键再移除旧键，重复绑定被拒绝；配置改为原子 JSON 写入并清洗 bool / int 键
- **色彩精度**：Bradford D50→D65 采用精确逆变换，Lab 往返漂移由 0.91 降至 0.0004；色环取值改用 `round()` 消除截断偏差

### 变更

- **行尾统一**：新增 `.gitattributes`，文本文件统一 LF（覆盖 Windows 编辑器 CRLF 改写），`.bat` 保持 CRLF，dll / png / ico / icns 标记为二进制
- **模块收敛**：新增 `core/__init__.py` 消除隐式命名空间包；单实例锁重试后再放弃；onefile 崩溃日志写入用户数据目录
- **构建防线**：`build_pyqt.py` 在打包前校验 `APP_VERSION` 与 `file_version_info.txt`、`release_notes.md` 一致，版本漂移直接失败；发布契约测试改为从 `APP_VERSION` 派生断言
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.7` / `1.6.7.0`

---

## v1.6.6

统一色彩模型、崩溃诊断与界面语言切换发布。

### 新增

- **统一色彩模型**：新增 `ui/color_model.py` 的 `Color` / `ColorState`，把 sRGB / HSV / HSL / CIELAB / OKLab / OKLCh 六种色空间的取色值收敛为单一不可变、恒在色域内的颜色快照；源色空间坐标精确往返，不再经 RGB 往返推导，消除 HSV→RGB→OKLCh 往返造成的约 0.2° 色相漂移
- **绝对 OKLCh 色度滑块**：C 滑块由「占最大色度比例」改为真实 OKLCh 色度坐标（0~0.321），色域映射统一在 `Color.from_space` 内完成；滑块轨道以灰色标出超出 sRGB 边界的部分
- **界面语言切换**：新增轻量「源文本即键」翻译层，设置中可在「自动 / 中文 / English」间切换界面语言，未翻译字符串自动回退中文
- **崩溃报告**：全局异常钩子在用户数据目录写入崩溃标记，下次启动时提示并定位 `stderr.log`
- **更新自替换**：更新检查模块新增自替换能力，单文件（onefile）版可原地替换自身并重启

### 修复

- **取色色相漂移**：HSV / OKLCh 之间反复切换导致的约 0.2° 色相漂移（源坐标经 RGB 往返后丢失精度）
- **无彩色色相丢失**：灰度 / 近黑取色时 RGB 无法携带色相信息导致色相跳变为 0°，现按 HSV 与 OKLCh 分别记忆色相并在无彩色时回填

### 变更

- **标量 HSV/HSL 转换收敛**：`ui/color_conversions.py` 新增与 `colorsys` 对齐的标量 HSV/HSL 转换助手，`ui/color_wheel.py` 等改用它，消除重复实现
- **主窗口 mixin 重构**：主窗口按职责拆分为托盘 / 同步 / 热键 / 色槽等独立 mixin，并抽取标题栏、渐变滑块等独立控件
- **滑块数值标签视觉简化**：滑块数值标签移除背景色块，仅绘制 +/- 步进箭头，数字更清晰
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.6` / `1.6.6.0`

---

## v1.6.5

透明色支持与 Photoshop 多实例 / 绿色版同步发布。

### 新增

- **透明色支持**：前景 / 背景色板下方新增透明色板按钮（棋盘格胶囊），点击将活动色槽标记为透明；CSP Companion 协议与 CSP 5.1 内存同步均支持透明标志透传，Photoshop 无 alpha 通道故跳过
- **CSP 主 / 副色槽同步**：CSP 5.1 主色与副色两个色槽独立同步（副色多内存副本定位后全量写入），CSP 内切换活动色槽不再互相覆盖 Colorink 的值
- **Photoshop 多实例与绿色版支持**：自动检测所有运行中的 Photoshop 实例（注册版走 COM / 绿色便携版走隐藏 CEP 扩展桥），COM 不可用时自动回退脚本桥，无需手动配置
- **Photoshop 前景 / 背景双色同步**：同步层独立读写 PS 前景与背景两个色槽

### 修复

- **CSP 5.1 主色读回错误**：用户在 CSP 内改色后 Colorink 读回的主色不正确（旧偏移 +0x20 是残留值，CSP 实际写入 +0x3C）；现按真实写入偏移读取
- **Photoshop COM 阻塞**：COM 注册在绿色版上偶发失效，Dispatch 可阻塞数十秒；现单次 COM 失败后快速回退脚本桥，不再拖住 UI 或同步循环
- **双击色板误交换**：快速双击前景 / 背景色板不再交换两槽颜色（双击仅切换编辑目标，首按已选中目标槽）

### 变更

- **开发诊断工具**：新增 `tools/` 目录，内置内存探针、写入路径对比与端到端稳定性脚本（`find_csp_subcolor` / `probe_sub_hsv` / `e2e_bidirectional` 等），便于排查同步问题
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.5` / `1.6.5.0`

---

## v1.6.4

CSP 5.1 笔刷颜色同步与标题栏显隐切换发布。

### 新增

- **CSP 5.1 笔刷颜色同步**：支持 CLIP STUDIO PAINT 5.1 的新全局颜色槽布局与紧凑 RGB 编码，自动检测文件版本并选择对应内存配置；设置中新增 `csp5.1` 版本选项，同步时同步更新 CSP 色环 HSV 状态
- **标题栏显隐切换**：新增全局快捷键 `Ctrl+Shift+T` 切换设置、最小化、关闭按钮所在的标题栏；隐藏后顶部边框与四周一致，内容区域自动重排；托盘菜单与设置页均提供显示标题栏控制

### 修复

- **关闭设置后无焦点模式未恢复**：启用无焦点模式时关闭设置窗口后，现立即重新应用主窗口标志与无焦点策略

### 变更

- **打包体积缩减**：剔除应用不会加载的 Qt、Pillow、numpy 组件与插件，Onedir / Onefile 构建统一执行精简清理
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.4` / `1.6.4.0`

---

## v1.6.3

修复打包版灰度滤镜失效问题。

### 修复

- **打包版灰度滤镜失效**：原生灰度后端依赖的 dxcam Cython 加速扩展 `_numpy_kernels` 通过动态字符串导入加载，PyInstaller 无法静态分析导致打包时遗漏；打包版运行时该后端回退到未内置的 cv2 处理器，捕获线程因 `cv2` 缺失崩溃，滤镜覆盖层无画面（源码版不受影响）。现已将该扩展显式加入打包清单，滤镜恢复正常
- **构建错误日志消除**：移除打包配置中已不存在的 `dxcam.core.capture` 隐藏导入（dxcam 0.3.x 已无此模块），构建不再输出 `Hidden import not found` 错误

### 变更

- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.3` / `1.6.3.0`

---

## v1.6.2

原生灰度后端能力补全（Luma 模式 + 按屏目标）与输入稳定性发布。

### 新增

- **原生灰度后端支持 Luma 模式**：原生捕获 + OpenGL 覆盖层现可选择 BT.709 Luma 灰度（此前仅 OKLCh 感知灰度），设置中切换后端为 Native 时可直接在模式下拉选择「Luma (BT.709 标准)」
- **灰度滤镜按屏目标**：Native 后端可选择黑白滤镜只作用于指定屏幕，设置中列出各显示器名称与分辨率；切换模式或屏幕目标时滤镜自动停止并重启生效，无需先关滤镜
- **数位板笔按键支持 LAB 切换**：LAB 切换快捷键支持数位板笔按键（如笔侧键绑定右键）；笔事件与其合成鼠标事件自动去重，不会触发双重切换

### 修复

- **缩放光标残留**：窗口边缘缩放光标在鼠标移出边缘区域或越过部分不跟踪鼠标的子控件后可能残留，现统一在移动 / 进入 / 离开事件中同步光标状态，并增加越界判断
- **Native 滤镜切换报错**：Native 滤镜运行中调用模式 / 屏幕目标切换会直接抛错（此前 Luma 仅限 Mag 后端、目标固定全部屏幕），现改为自动重启并应用新设置

### 变更

- **灰度设置选项随后端联动**：灰度模式与屏幕目标下拉框根据所选后端动态更新（Mag 固定为系统级全屏 Luma，Native 可选 OKLCh / Luma 与指定屏幕），配置保存逻辑收敛为单一映射函数
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.2` / `1.6.2.0`

---

## v1.6.1

原生 OKLCh 灰度后端与 LAB 视图快速切换发布。

### 新增

- **原生 OKLCh 灰度后端**：基于 DXGI 桌面复制 + OpenGL OKLab-L 着色器的纯 Python 全屏灰度实现，替代原有 C++ overlay 栈（DComp 直通 / 屏幕捕获 overlay / ShaderGlass / Rust D3D11），无独立原生进程与二进制，打包体更小、依赖更一致
- **LAB 视图快速切换**：鼠标悬停色轮 / LAB 区域时按 `Space`（可改绑鼠标侧键 / 中键）即可切换视图；新增全局热键 `Ctrl+L` 任意位置切换；设置中可显示色轮与 LAB 之间的浮动切换按钮
- **全局热键支持鼠标按键**：所有快捷键均可绑定鼠标侧键 / 中键等；全局鼠标热键不拦截点击，绘画软件仍能收到该点击
- **灰度滤镜启动预热**：应用启动后在后台预导入 dxcam 并预热 OpenGL 渲染链，首次 `Ctrl+G` 无需再等待一次性初始化开销

### 修复

- **首次切换灰度滤镜延迟**：首次 `Ctrl+G` 曾需约 1 秒（dxcam 导入 + 逐屏串行创建相机），且陈旧相机场景下最坏约 5 秒失败；现改为启动时后台预导入、按屏并行异步创建相机，切换接近即时
- **陈旧相机导致切换失败**：dxcam 偶发返回上一会话未释放的相机，此前 5×1 秒重试仍可能失败；现强制释放陈旧相机并快速重试（3×0.2 秒），失败立即明确提示，不再静默等待

### 变更

- **灰度后端选项收敛**：设置中仅保留「OKLCh (GPU兼容)」与「系统 Luma (Mag)」两项，移除 DComp 直通、Rust D3D11、OpenGL Overlay 等旧后端及其原生程序（`dcomp_overlay` / `sc_overlay` / `mhc2_overlay` / `shaderglass` / `screen_filter.exe`）
- **默认后端切换为 native**：`grayscaleFilterBackend` 默认值由 `overlay` 改为 `native`，旧配置中引用已移除后端时自动迁移到 `native`
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.1` / `1.6.1.0`

---

## v1.6.0

系统级灰度后端与渲染稳定化发布。

### 新增

- **系统级 (Mag) 灰度后端**：基于 Windows 放大镜 API（MagSetFullscreenColorEffect）在 DWM 合成层应用 5×5 颜色矩阵，与系统自带颜色滤镜同路径——零捕获、零覆盖窗口、无额外 GPU 开销；不修改显示器 ICC 配置文件，避免颜色管理路径导致的灰度偏色；鼠标光标不受矩阵影响。仅提供 BT.709 Luma 灰度模式
- **灰度滤镜失败明确提示**：所选渲染后端无法运行时不再静默切换，而是弹出具体错误原因，用户可在设置中手动更换后端
- **Photoshop 同步失败原因诊断**：设置页同步状态直接显示连接失败原因（进程未启动 / COM 接口未注册 / UAC 权限不匹配）；检测到 Photoshop 以管理员身份运行而 Colorink 未提权时，提供一键「以管理员身份重启」

### 修复

- **DComp 直通帧率受限**：DWM 将 GUI 子系统进程的全屏呈现限速至约 20Hz，重写为单进程 WGC 零拷贝捕获 + Present(0, ALLOW_TEARING) 异步呈现，并改用控制台子系统规避限速，实测恢复 60fps 恒定刷新
- **OpenGL 滤镜静默回退误导**：OpenGL 滤镜失效时此前会自动回退到 DComp 并改写配置，用户不知情；现在保持所选后端并明确提示手动切换

### 变更

- **灰度模式选项按后端过滤**：设置中切换渲染后端时，黑白模式列表只显示该后端支持的模式（Rust 仅 OKLCh、Mag 仅 Luma），避免选择无效组合
- **DComp 直通架构重构**：由双进程捕获 + 呈现合并为单进程 WGC 捕获，加入 WDA_EXCLUDEFROMCAPTURE 防捕获反馈
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.0` / `1.6.0.0`

---

## v1.5.2

OKLCh 切片宽幅化与窗口紧凑适配发布。

### 新增

- **OKLCh 切片按色相自适应宽度**：每个色相的色域都独立缩放并填满切片框宽度，彩色区域不再受最宽色相限制，始终与切片框同宽；Ringless 模式下切片框扩展到整行可用宽度，取色区域更大、更容易精准取色
- **设置窗口紧凑化**：设置窗口由 560px 收窄至 460px，左侧导航栏由 112px 收窄至 96px，整体更紧凑；导航图标改为 72px 高清画布绘制，HiDPI 下线条更锐利

### 修复

- **色轮下弧裁剪**：窗口较矮较宽（手动缩放、低屏幕空间）时色轮下半弧会被裁出可视区，现在色轮尺寸按窗格高度钳制，超出时缩小而不是裁剪
- **OKLCh 色域轮廓丢失**：色相变化后预渲染切片落地时，色域边界描边会消失，现在预渲染图像上方仍会绘制色域轮廓
- **OKLCh 指示器越界**：切换色相后存储的亮度 / 彩度可能超出新色相的最大彩度，指示点会飘到彩色区域之外，现在会钳制在色域边界内
- **预渲染与控件不一致**：后台预渲染使用 21 点彩度扫描而控件使用 201 点，导致缓存图与轮廓描边、指示器比例错位，统一为 201 点扫描并按切片框宽度缩放，两者完全一致

### 变更

- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.2` / `1.5.2.0`

---

## v1.5.1

设置面板导航重构与稳定性修复发布。

### 新增

- **设置面板左侧导航栏**：设置界面由标签页改为 CSP 环境设置风格的左侧导航栏 + 内容页布局；导航图标由 QPainter 按当前主题绘制并支持 HiDPI 高清渲染
- **取色器设置整理**：取色放大倍率与前背景色位置设置移入取色器分区，色轮分区仅保留可视化相关选项；「高级」改为扁平分区展示

### 修复

- **配置目录回退**：系统环境变量 `APPDATA` 未设置时回退到用户主目录，避免配置文件读写失败
- **CSP 内存同步**：`pymem` 不可用或模块未找到时给出明确错误，不再静默失败
- **Photoshop 同步**：连接流程重构并补充进程存活检查，避免对已退出的 PS 发起 COM 调用
- **Rust 灰度滤镜**：子进程 stdin 不可用时安全跳过，不再抛出异常
- **主窗口空值防护**：修复多显示器、系统样式缺失等边界场景下的潜在崩溃（取色定位、拖拽缩放、托盘图标、灰度后端切换）

### 变更

- **代码现代化**：类型标注全面迁移至内建泛型（`dict` / `tuple`）与 `X | None` 语法，统一导入排序
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.1` / `1.5.1.0`

---

## v1.5.0

无环取色（Ringless）模式与设置窗口重构发布。

### 新增

- **Ringless 模式**：可隐藏色环并放大颜色切片，主界面切换为矩形前景 / 背景色块与顶部控制栏布局；控制栏支持前景 / 背景槽、模块与模式按钮，按钮位置按共享基线对齐，切换面板时不再跳动
- **颜色预览框**：独立的前景 / 背景双色块组件，统一绘制与命中测试几何，支持右键菜单快速复制 RGB / HEX 值
- **独立设置窗口**：设置界面改为带标题栏的独立无边框窗口，可拖动、跟随主窗口贴靠并自动限位；设置侧栏支持分区滚动与更细粒度的模块控制
- **切片与 LAB 预渲染**：颜色切片和 LAB 可视化改为后台线程预渲染，切换模块时即时显示缓存结果，减少交互卡顿
- **热键按钮组件**：设置中的热键录入改为专用按钮组件，交互状态更清晰

### 变更

- **色轮与颜色转换重构**：色轮绘制、OKLab / OKLCh 转换管道全面重构，消除 RGB 往返漂移，提升绘制一致性
- **图标资源**：新增复选框、下拉箭头等主题图标资源
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.0` / `1.5.0.0`

---

## v1.4.0

围绕 CSP Companion、色空间模块和打包发布一致性的一次功能发布。

### 新增

- **CSP Companion Mode**：新增基于 CLIP STUDIO PAINT「连接智能手机」协议的 TCP 同步后端，可在不依赖内存扫描和版本偏移的情况下读写 CSP 颜色；支持保存连接会话、重新连接、断开连接，以及二维码扫描失败时手动粘贴连接 URL
- **色空间模块切换**：新增 HSV / HLS / LCH 三个色空间模块，每个模块预设对应色轮和滑块组合；模块切换按钮可显示在主界面，也可在设置中关闭
- **OKLab / OKLCh 交互增强**：改进 OKLab、OKLCh 滑块、色域范围和亮度拖动逻辑，减少 RGB 往返导致的色相/饱和度漂移
- **内容驱动窗口高度**：窗口高度按当前可见内容自动收缩或扩展，减少首次打开和切换模块后的空白区域
- **发布契约测试**：新增版本一致性、首次启动默认值和配置合并测试，防止应用版本、Windows 文件版本和发布说明脱节

### 修复

- **首次启动默认值**：默认热键统一为 `F11` / `Ctrl+R` / `Ctrl+H` / `Ctrl+G`，与 README 和设置界面一致；默认仅展示紧凑的 HSV 模块与颜色历史，历史色板为 `8 × 2`
- **Brush Link 精度**：CSP / UDM 写入 HSV 时保留低饱和度和黑色场景下的色相、饱和度状态，降低同步到绘画软件后的颜色跳变
- **滑块模块联动**：设置面板现在按当前色空间模块过滤滑块行，避免显示不属于当前模块的滑块组
- **打包崩溃排查**：新增全局异常日志输出到 `stderr.log`，便于定位打包后未捕获异常

### 变更

- **版本统一**：应用内更新检查版本与 Windows 文件版本统一到 `1.4.0` / `1.4.0.0`
- **打包说明**：README 补充 Onedir / Onefile 输出说明，以及 CSP Companion 二维码扫描依赖

---

## v1.3.0

Rust D3D11 灰度滤镜后端和打包瘦身发布。

### 新增

- **Rust D3D11 灰度滤镜**：新增 Rust D3D11 后端并集成到设置界面，可作为灰度滤镜渲染后端选择

### 变更

- **构建配置**：移除废弃 D3D11 Overlay 和调试 spec，更新 PyInstaller 配置并移除 OpenCV 运行依赖，减小打包体积
- **版本号**：Windows 文件版本升级到 `1.3.0.0`

---

## v1.2.2

修复 Photoshop 颜色同步卡死和取色历史丢失。

### 修复

- **Photoshop 颜色同步**：调整 COM ProgID 查询顺序，优先使用版本无关的 `Photoshop.Application` 而非残留的 `Photoshop.Application.140`（该废弃条目每次重连会阻塞 ~30 秒），同步延迟从数十秒降至毫秒级
- **取色历史**：外部软件（CSP/SAI/UDM/PS）颜色同步变化时，现在正确录入取色历史，不再丢失外部取色的记录
- **滑块跟手**：拖动滑块/色轮时，滑块轨道渐变色和 L 色域遮罩改用延迟渲染，手柄始终紧跟光标不再卡顿

---

## v1.2.1

设置面板新增「关于」一栏：检查更新 / 关于作者。

### 新增
- **检查更新**：后台线程查询 GitHub releases，对比 `APP_VERSION` 与最新 tag；发现新版本时弹出公告并附带跳转下载按钮，已是最新则提示当前版本
- **关于作者**：打开作者 Bilibili 主页
- 新增 `core/updater.py`（仅依赖标准库 `urllib` / `json`，不影响打包体积）
- `file_version_info.txt` 同步升级到 `1.2.1.0`

---

## v1.2.0

新增全局取色功能，并修复 OKLCh 的 L 值条。

### 新增

- **全局取色**：在任意位置通过热键（默认 `F11`）触发全屏取色放大镜
  - 截图思路改造为「先全屏静态截图，再从图中取色」，确保预览里再也不会出现小窗口自己、其它悬浮窗或叠加层的边缘
  - 高分屏 / 多显示器 / 不同 DPR 自动适配
  - 视觉上隐藏系统光标，仅保留细十字辅助点跟随鼠标
  - 自定义十字辅助点在所有屏幕（含高 DPI 主屏）下均完整显示

### 修复

- **OKLCh 的 L 值条**：滑块联动不再错位 / 跳变

---

## v1.1.1

Fix: grayscale overlay (OpenGL / Ctrl+G) broken after PyInstaller packaging.

### Root Cause
dxcam dynamically imports OpenCV via `importlib.import_module("cv2")` which PyInstaller's static analysis cannot detect. This causes `ModuleNotFoundError: No module named 'cv2'` at runtime, crashing the frame processing thread.

Additional issues: `GrayscaleOverlay` missing `is_healthy` attribute (AttributeError), dxcam C extension (`_numpy_kernels.pyd`) not bundled, and overlay C++ EXEs not included.

### Changes
- **ui/grayscale_overlay.py**: Added `is_healthy` property + QTimer-based delayed health check (2.5s after overlay creation)
- **core/dcomp_grayscale.py**: Fixed DComp EXE path lookup for PyInstaller-frozen environments (`sys._MEIPASS`)
- **PyInstaller specs**: Added hidden imports (`cv2`, all dxcam submodules, `comtypes`), explicitly bundled dxcam C extension `.pyd` and `dcomp_overlay.exe`/`sc_overlay.exe`
- **build_pyqt.py**: Now builds both onedir (folder) and onefile (single EXE) outputs simultaneously
