## v1.6.8

设置页重组与代码模块化重构发布。

### 新增

- **设置页重组**：导航由 5 页调整为 6 页（快捷键 / 界面 / 取色器 / 滤镜 / 同步 / 关于），灰度滤镜设置独立为「滤镜」页，绘画软件同步独立为「同步」页；界面语言切换移入「界面」页
- **onedir ZIP 更新支持**：更新检查可识别 `Colorink-Onedir.zip` 便携目录资产，onedir 安装不再误下 GitHub 源码 zip，找不到 onedir zip 时自动回退 EXE
- **界面文案补全**：补齐 Ringless、滤镜、同步、无焦点取色等场景的中英文翻译，减少英文界面下未翻译回退

### 修复

- **取色历史框线**：悬停 / 选中框改为独立覆盖层绘制，修复高分屏下历史格接缝处蓝框断裂、错位与残留
- **历史格选中同步**：颜色历史写入统一色彩模型时补 `QColor` 包装，修复选中态在部分场景下不刷新
- **设置重组回归**：同步与版本信息同卡、语言组合框归属、滤镜后端选项等随页面拆分同步修正，全部原设置项保留

### 变更

- **模块化重构**：`core/csp_brush_link.py` 拆为 `core/csp_brush_link` 包；`ui/color_wheel.py` 拆为 geometry / interaction / prewarm / rendering；`ui/main_window.py` 拆为 `ui.window` 关注点 mixin；`ui/settings_sidebar.py` 拆为 `ui.settings` 面板；对外导入路径保持兼容
- **文案统一**：诊断信息中的「同步软件」改为「同步目标」；「黑白滤镜」「无焦点选色模式」等文案统一为「灰度滤镜」「无焦点取色模式」
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.8` / `1.6.8.0`

---

## v1.6.7

更新检查加固与灰度滤镜可靠性修复发布。

### 新增

- **更新检查加固**：安装包下载改为先落 `.part` 临时文件，按字节数 + SHA-256 校验通过后原子改名；支持可选 `COLORINK_GITHUB_TOKEN` 请求头，403 / 404 / 网络错误分别携带 `error_detail` 结构化报错
- **双形态安装包识别**：自动区分 onedir / onefile 两种构建形态并选择对应资产（onefile 取体积最大的 EXE），自替换前额外校验 EXE 所在目录可写
- **跳过版本**：手动与托盘更新检查共用同一流程，可记录并跳过指定版本；托盘菜单与设置页均接入应用内下载流程
- **灰度运行时纳入仓库**：`native_grayscale/runtime/grayscale_overlay.pyc` 随仓库分发，`build_runtime.py` 可复现编译；原生灰度不可用时自动回退 Mag 后端

### 修复

- **灰度滤镜无法激活**：运行时用 GUI 线程信号桥替换从未注册进元对象的后注入 `pyqtSlot`，相机 kick 真正触发；屏幕布局 / DPI 变化时重新应用覆盖层几何；健康检查杀掉实现后不再永久卡在 `_warming`
- **托盘崩溃与空转**：持有上下文 QMenu 引用（原被 GC 导致右键悬空指针）；修复单击 / 双击重复切换；启动更新检查结果改由排队信号投递到 GUI 线程；窗口隐藏期间不再武装 0ms 高度定时器（原会空转占满单核）
- **同步断连不恢复**：CSP 5.1 黑白降级副本不再被缓存（镜像降级态不会永久锁定）；CSP / SAI2 / UDM 连续读失败计数清零后丢弃过期连接，软件重启后自动重连
- **64 位句柄截断**：PS 同步、前台检测、SAI2 等 ctypes 调用补齐 `restype` / `argtypes`（`OpenProcess` / `WaitForSingleObject` / `CloseHandle`）
- **热键与配置健壮性**：捕获不支持按键不再清空已绑定热键，重绑先注册新键再移除旧键，重复绑定被拒绝；配置改为原子 JSON 写入并清洗 bool / int 键
- **色彩精度**：Bradford D50→D65 采用精确逆变换，Lab 往返漂移由 0.91 降至 0.0004；色环取值改用 `round()` 消除截断偏差

### 变更

- **行尾统一**：新增 `.gitattributes`，文本文件统一 LF（覆盖 Windows 编辑器 CRLF 改写），`.bat` 保持 CRLF，dll / png / ico / icns 标记为二进制
- **模块收敛**：新增 `core/__init__.py` 消除隐式命名空间包；单实例锁重试后再放弃；onefile 崩溃日志写入用户数据目录
- **构建防线**：`build_pyqt.py` 在打包前校验 `APP_VERSION` 与 `file_version_info.txt`、`release_notes.md` 一致，版本漂移直接失败；发布契约测试改为从 `APP_VERSION` 派生断言
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.7` / `1.6.7.0`

---

## v1.6.6

统一色彩模型、崩溃诊断与界面语言切换发布。

### 新增

- **统一色彩模型**：新增 `ui/color_model.py` 的 `Color` / `ColorState`，把 sRGB / HSV / HSL / CIELAB / OKLab / OKLCh 六种色空间的取色值收敛为单一不可变、恒在色域内的颜色快照；源色空间坐标精确往返，不再经 RGB 往返推导，消除 HSV→RGB→OKLCh 往返造成的约 0.2° 色相漂移
- **绝对 OKLCh 色度滑块**：C 滑块由「占最大色度比例」改为真实 OKLCh 色度坐标（0~0.321），色域映射统一在 `Color.from_space` 内完成；滑块轨道以灰色标出超出 sRGB 边界的部分
- **界面语言切换**：新增轻量「源文本即键」翻译层，设置中可在「自动 / 中文 / English」间切换界面语言，未翻译字符串自动回退中文
- **崩溃报告**：全局异常钩子在用户数据目录写入崩溃标记，下次启动时提示并定位 `stderr.log`
- **更新自替换**：更新检查模块新增自替换能力，单文件（onefile）版可原地替换自身并重启

### 修复

- **取色色相漂移**：HSV / OKLCh 之间反复切换导致的约 0.2° 色相漂移（源坐标经 RGB 往返后丢失精度）
- **无彩色色相丢失**：灰度 / 近黑取色时 RGB 无法携带色相信息导致色相跳变为 0°，现按 HSV 与 OKLCh 分别记忆色相并在无彩色时回填

### 变更

- **标量 HSV/HSL 转换收敛**：`ui/color_conversions.py` 新增与 `colorsys` 对齐的标量 HSV/HSL 转换助手，`ui/color_wheel.py` 等改用它，消除重复实现
- **主窗口 mixin 重构**：主窗口按职责拆分为托盘 / 同步 / 热键 / 色槽等独立 mixin，并抽取标题栏、渐变滑块等独立控件
- **滑块数值标签视觉简化**：滑块数值标签移除背景色块，仅绘制 +/- 步进箭头，数字更清晰
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.6` / `1.6.6.0`

---

## v1.6.5

透明色支持与 Photoshop 多实例 / 绿色版同步发布。

### 新增

- **透明色支持**：前景 / 背景色板下方新增透明色板按钮（棋盘格胶囊），点击将活动色槽标记为透明；CSP Companion 协议与 CSP 5.1 内存同步均支持透明标志透传，Photoshop 无 alpha 通道故跳过
- **CSP 主 / 副色槽同步**：CSP 5.1 主色与副色两个色槽独立同步（副色多内存副本定位后全量写入），CSP 内切换活动色槽不再互相覆盖 Colorink 的值
- **Photoshop 多实例与绿色版支持**：自动检测所有运行中的 Photoshop 实例（注册版走 COM / 绿色便携版走隐藏 CEP 扩展桥），COM 不可用时自动回退脚本桥，无需手动配置
- **Photoshop 前景 / 背景双色同步**：同步层独立读写 PS 前景与背景两个色槽

### 修复

- **CSP 5.1 主色读回错误**：用户在 CSP 内改色后 Colorink 读回的主色不正确（旧偏移 +0x20 是残留值，CSP 实际写入 +0x3C）；现按真实写入偏移读取
- **Photoshop COM 阻塞**：COM 注册在绿色版上偶发失效，Dispatch 可阻塞数十秒；现单次 COM 失败后快速回退脚本桥，不再拖住 UI 或同步循环
- **双击色板误交换**：快速双击前景 / 背景色板不再交换两槽颜色（双击仅切换编辑目标，首按已选中目标槽）

### 变更

- **开发诊断工具**：新增 `tools/` 目录，内置内存探针、写入路径对比与端到端稳定性脚本（`find_csp_subcolor` / `probe_sub_hsv` / `e2e_bidirectional` 等），便于排查同步问题
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.5` / `1.6.5.0`

---

## v1.6.4

CSP 5.1 笔刷颜色同步与标题栏显隐切换发布。

### 新增

- **CSP 5.1 笔刷颜色同步**：支持 CLIP STUDIO PAINT 5.1 的新全局颜色槽布局与紧凑 RGB 编码，自动检测文件版本并选择对应内存配置；设置中新增 `csp5.1` 版本选项，同步时同步更新 CSP 色环 HSV 状态
- **标题栏显隐切换**：新增全局快捷键 `Ctrl+Shift+T` 切换设置、最小化、关闭按钮所在的标题栏；隐藏后顶部边框与四周一致，内容区域自动重排；托盘菜单与设置页均提供显示标题栏控制

### 修复

- **关闭设置后无焦点模式未恢复**：启用无焦点模式时关闭设置窗口后，现立即重新应用主窗口标志与无焦点策略

### 变更

- **打包体积缩减**：剔除应用不会加载的 Qt、Pillow、numpy 组件与插件，Onedir / Onefile 构建统一执行精简清理
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.4` / `1.6.4.0`

---

## v1.6.3

修复打包版灰度滤镜失效问题。

### 修复

- **打包版灰度滤镜失效**：原生灰度后端依赖的 dxcam Cython 加速扩展 `_numpy_kernels` 通过动态字符串导入加载，PyInstaller 无法静态分析导致打包时遗漏；打包版运行时该后端回退到未内置的 cv2 处理器，捕获线程因 `cv2` 缺失崩溃，滤镜覆盖层无画面（源码版不受影响）。现已将该扩展显式加入打包清单，滤镜恢复正常
- **构建错误日志消除**：移除打包配置中已不存在的 `dxcam.core.capture` 隐藏导入（dxcam 0.3.x 已无此模块），构建不再输出 `Hidden import not found` 错误

### 变更

- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.3` / `1.6.3.0`

---

## v1.6.2

原生灰度后端能力补全（Luma 模式 + 按屏目标）与输入稳定性发布。

### 新增

- **原生灰度后端支持 Luma 模式**：原生捕获 + OpenGL 覆盖层现可选择 BT.709 Luma 灰度（此前仅 OKLCh 感知灰度），设置中切换后端为 Native 时可直接在模式下拉选择「Luma (BT.709 标准)」
- **灰度滤镜按屏目标**：Native 后端可选择黑白滤镜只作用于指定屏幕，设置中列出各显示器名称与分辨率；切换模式或屏幕目标时滤镜自动停止并重启生效，无需先关滤镜
- **数位板笔按键支持 LAB 切换**：LAB 切换快捷键支持数位板笔按键（如笔侧键绑定右键）；笔事件与其合成鼠标事件自动去重，不会触发双重切换

### 修复

- **缩放光标残留**：窗口边缘缩放光标在鼠标移出边缘区域或越过部分不跟踪鼠标的子控件后可能残留，现统一在移动 / 进入 / 离开事件中同步光标状态，并增加越界判断
- **Native 滤镜切换报错**：Native 滤镜运行中调用模式 / 屏幕目标切换会直接抛错（此前 Luma 仅限 Mag 后端、目标固定全部屏幕），现改为自动重启并应用新设置

### 变更

- **灰度设置选项随后端联动**：灰度模式与屏幕目标下拉框根据所选后端动态更新（Mag 固定为系统级全屏 Luma，Native 可选 OKLCh / Luma 与指定屏幕），配置保存逻辑收敛为单一映射函数
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.2` / `1.6.2.0`

---

## v1.6.1

原生 OKLCh 灰度后端与 LAB 视图快速切换发布。

### 新增

- **原生 OKLCh 灰度后端**：基于 DXGI 桌面复制 + OpenGL OKLab-L 着色器的纯 Python 全屏灰度实现，替代原有 C++ overlay 栈（DComp 直通 / 屏幕捕获 overlay / ShaderGlass / Rust D3D11），无独立原生进程与二进制，打包体更小、依赖更一致
- **LAB 视图快速切换**：鼠标悬停色轮 / LAB 区域时按 `Space`（可改绑鼠标侧键 / 中键）即可切换视图；新增全局热键 `Ctrl+L` 任意位置切换；设置中可显示色轮与 LAB 之间的浮动切换按钮
- **全局热键支持鼠标按键**：所有快捷键均可绑定鼠标侧键 / 中键等；全局鼠标热键不拦截点击，绘画软件仍能收到该点击
- **灰度滤镜启动预热**：应用启动后在后台预导入 dxcam 并预热 OpenGL 渲染链，首次 `Ctrl+G` 无需再等待一次性初始化开销

### 修复

- **首次切换灰度滤镜延迟**：首次 `Ctrl+G` 曾需约 1 秒（dxcam 导入 + 逐屏串行创建相机），且陈旧相机场景下最坏约 5 秒失败；现改为启动时后台预导入、按屏并行异步创建相机，切换接近即时
- **陈旧相机导致切换失败**：dxcam 偶发返回上一会话未释放的相机，此前 5×1 秒重试仍可能失败；现强制释放陈旧相机并快速重试（3×0.2 秒），失败立即明确提示，不再静默等待

### 变更

- **灰度后端选项收敛**：设置中仅保留「OKLCh (GPU兼容)」与「系统 Luma (Mag)」两项，移除 DComp 直通、Rust D3D11、OpenGL Overlay 等旧后端及其原生程序（`dcomp_overlay` / `sc_overlay` / `mhc2_overlay` / `shaderglass` / `screen_filter.exe`）
- **默认后端切换为 native**：`grayscaleFilterBackend` 默认值由 `overlay` 改为 `native`，旧配置中引用已移除后端时自动迁移到 `native`
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.1` / `1.6.1.0`

---

## v1.6.0

系统级灰度后端与渲染稳定化发布。

### 新增

- **系统级 (Mag) 灰度后端**：基于 Windows 放大镜 API（MagSetFullscreenColorEffect）在 DWM 合成层应用 5×5 颜色矩阵，与系统自带颜色滤镜同路径——零捕获、零覆盖窗口、无额外 GPU 开销；不修改显示器 ICC 配置文件，避免颜色管理路径导致的灰度偏色；鼠标光标不受矩阵影响。仅提供 BT.709 Luma 灰度模式
- **灰度滤镜失败明确提示**：所选渲染后端无法运行时不再静默切换，而是弹出具体错误原因，用户可在设置中手动更换后端
- **Photoshop 同步失败原因诊断**：设置页同步状态直接显示连接失败原因（进程未启动 / COM 接口未注册 / UAC 权限不匹配）；检测到 Photoshop 以管理员身份运行而 Colorink 未提权时，提供一键「以管理员身份重启」

### 修复

- **DComp 直通帧率受限**：DWM 将 GUI 子系统进程的全屏呈现限速至约 20Hz，重写为单进程 WGC 零拷贝捕获 + Present(0, ALLOW_TEARING) 异步呈现，并改用控制台子系统规避限速，实测恢复 60fps 恒定刷新
- **OpenGL 滤镜静默回退误导**：OpenGL 滤镜失效时此前会自动回退到 DComp 并改写配置，用户不知情；现在保持所选后端并明确提示手动切换

### 变更

- **灰度模式选项按后端过滤**：设置中切换渲染后端时，黑白模式列表只显示该后端支持的模式（Rust 仅 OKLCh、Mag 仅 Luma），避免选择无效组合
- **DComp 直通架构重构**：由双进程捕获 + 呈现合并为单进程 WGC 捕获，加入 WDA_EXCLUDEFROMCAPTURE 防捕获反馈
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.6.0` / `1.6.0.0`

---

## v1.5.2

OKLCh 切片宽幅化与窗口紧凑适配发布。

### 新增

- **OKLCh 切片按色相自适应宽度**：每个色相的色域都独立缩放并填满切片框宽度，彩色区域不再受最宽色相限制，始终与切片框同宽；Ringless 模式下切片框扩展到整行可用宽度，取色区域更大、更容易精准取色
- **设置窗口紧凑化**：设置窗口由 560px 收窄至 460px，左侧导航栏由 112px 收窄至 96px，整体更紧凑；导航图标改为 72px 高清画布绘制，HiDPI 下线条更锐利

### 修复

- **色轮下弧裁剪**：窗口较矮较宽（手动缩放、低屏幕空间）时色轮下半弧会被裁出可视区，现在色轮尺寸按窗格高度钳制，超出时缩小而不是裁剪
- **OKLCh 色域轮廓丢失**：色相变化后预渲染切片落地时，色域边界描边会消失，现在预渲染图像上方仍会绘制色域轮廓
- **OKLCh 指示器越界**：切换色相后存储的亮度 / 彩度可能超出新色相的最大彩度，指示点会飘到彩色区域之外，现在会钳制在色域边界内
- **预渲染与控件不一致**：后台预渲染使用 21 点彩度扫描而控件使用 201 点，导致缓存图与轮廓描边、指示器比例错位，统一为 201 点扫描并按切片框宽度缩放，两者完全一致

### 变更

- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.2` / `1.5.2.0`

---

## v1.5.1

设置面板导航重构与稳定性修复发布。

### 新增

- **设置面板左侧导航栏**：设置界面由标签页改为 CSP 环境设置风格的左侧导航栏 + 内容页布局；导航图标由 QPainter 按当前主题绘制并支持 HiDPI 高清渲染
- **取色器设置整理**：取色放大倍率与前背景色位置设置移入取色器分区，色轮分区仅保留可视化相关选项；「高级」改为扁平分区展示

### 修复

- **配置目录回退**：系统环境变量 `APPDATA` 未设置时回退到用户主目录，避免配置文件读写失败
- **CSP 内存同步**：`pymem` 不可用或模块未找到时给出明确错误，不再静默失败
- **Photoshop 同步**：连接流程重构并补充进程存活检查，避免对已退出的 PS 发起 COM 调用
- **Rust 灰度滤镜**：子进程 stdin 不可用时安全跳过，不再抛出异常
- **主窗口空值防护**：修复多显示器、系统样式缺失等边界场景下的潜在崩溃（取色定位、拖拽缩放、托盘图标、灰度后端切换）

### 变更

- **代码现代化**：类型标注全面迁移至内建泛型（`dict` / `tuple`）与 `X | None` 语法，统一导入排序
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.1` / `1.5.1.0`

---

## v1.5.0

无环取色（Ringless）模式与设置窗口重构发布。

### 新增

- **Ringless 模式**：可隐藏色环并放大颜色切片，主界面切换为矩形前景 / 背景色块与顶部控制栏布局；控制栏支持前景 / 背景槽、模块与模式按钮，按钮位置按共享基线对齐，切换面板时不再跳动
- **颜色预览框**：独立的前景 / 背景双色块组件，统一绘制与命中测试几何，支持右键菜单快速复制 RGB / HEX 值
- **独立设置窗口**：设置界面改为带标题栏的独立无边框窗口，可拖动、跟随主窗口贴靠并自动限位；设置侧栏支持分区滚动与更细粒度的模块控制
- **切片与 LAB 预渲染**：颜色切片和 LAB 可视化改为后台线程预渲染，切换模块时即时显示缓存结果，减少交互卡顿
- **热键按钮组件**：设置中的热键录入改为专用按钮组件，交互状态更清晰

### 变更

- **色轮与颜色转换重构**：色轮绘制、OKLab / OKLCh 转换管道全面重构，消除 RGB 往返漂移，提升绘制一致性
- **图标资源**：新增复选框、下拉箭头等主题图标资源
- **版本号**：应用内更新检查版本与 Windows 文件版本统一到 `1.5.0` / `1.5.0.0`

---

## v1.4.0

围绕 CSP Companion、色空间模块和打包发布一致性的一次功能发布。

### 新增

- **CSP Companion Mode**：新增基于 CLIP STUDIO PAINT「连接智能手机」协议的 TCP 同步后端，可在不依赖内存扫描和版本偏移的情况下读写 CSP 颜色；支持保存连接会话、重新连接、断开连接，以及二维码扫描失败时手动粘贴连接 URL
- **色空间模块切换**：新增 HSV / HLS / LCH 三个色空间模块，每个模块预设对应色轮和滑块组合；模块切换按钮可显示在主界面，也可在设置中关闭
- **OKLab / OKLCh 交互增强**：改进 OKLab、OKLCh 滑块、色域范围和亮度拖动逻辑，减少 RGB 往返导致的色相/饱和度漂移
- **内容驱动窗口高度**：窗口高度按当前可见内容自动收缩或扩展，减少首次打开和切换模块后的空白区域
- **发布契约测试**：新增版本一致性、首次启动默认值和配置合并测试，防止应用版本、Windows 文件版本和发布说明脱节

### 修复

- **首次启动默认值**：默认热键统一为 `F11` / `Ctrl+R` / `Ctrl+H` / `Ctrl+G`，与 README 和设置界面一致；默认仅展示紧凑的 HSV 模块与颜色历史，历史色板为 `8 × 2`
- **Brush Link 精度**：CSP / UDM 写入 HSV 时保留低饱和度和黑色场景下的色相、饱和度状态，降低同步到绘画软件后的颜色跳变
- **滑块模块联动**：设置面板现在按当前色空间模块过滤滑块行，避免显示不属于当前模块的滑块组
- **打包崩溃排查**：新增全局异常日志输出到 `stderr.log`，便于定位打包后未捕获异常

### 变更

- **版本统一**：应用内更新检查版本与 Windows 文件版本统一到 `1.4.0` / `1.4.0.0`
- **打包说明**：README 补充 Onedir / Onefile 输出说明，以及 CSP Companion 二维码扫描依赖

---

## v1.3.0

Rust D3D11 灰度滤镜后端和打包瘦身发布。

### 新增

- **Rust D3D11 灰度滤镜**：新增 Rust D3D11 后端并集成到设置界面，可作为灰度滤镜渲染后端选择

### 变更

- **构建配置**：移除废弃 D3D11 Overlay 和调试 spec，更新 PyInstaller 配置并移除 OpenCV 运行依赖，减小打包体积
- **版本号**：Windows 文件版本升级到 `1.3.0.0`

---

## v1.2.2

修复 Photoshop 颜色同步卡死和取色历史丢失。

### 修复

- **Photoshop 颜色同步**：调整 COM ProgID 查询顺序，优先使用版本无关的 `Photoshop.Application` 而非残留的 `Photoshop.Application.140`（该废弃条目每次重连会阻塞 ~30 秒），同步延迟从数十秒降至毫秒级
- **取色历史**：外部软件（CSP/SAI/UDM/PS）颜色同步变化时，现在正确录入取色历史，不再丢失外部取色的记录
- **滑块跟手**：拖动滑块/色轮时，滑块轨道渐变色和 L 色域遮罩改用延迟渲染，手柄始终紧跟光标不再卡顿

---

## v1.2.1

设置面板新增「关于」一栏：检查更新 / 关于作者。

### 新增
- **检查更新**：后台线程查询 GitHub releases，对比 `APP_VERSION` 与最新 tag；发现新版本时弹出公告并附带跳转下载按钮，已是最新则提示当前版本
- **关于作者**：打开作者 Bilibili 主页
- 新增 `core/updater.py`（仅依赖标准库 `urllib` / `json`，不影响打包体积）
- `file_version_info.txt` 同步升级到 `1.2.1.0`

---

## v1.2.0

新增全局取色功能，并修复 OKLCh 的 L 值条。

### 新增

- **全局取色**：在任意位置通过热键（默认 `F11`）触发全屏取色放大镜
  - 截图思路改造为「先全屏静态截图，再从图中取色」，确保预览里再也不会出现小窗口自己、其它悬浮窗或叠加层的边缘
  - 高分屏 / 多显示器 / 不同 DPR 自动适配
  - 视觉上隐藏系统光标，仅保留细十字辅助点跟随鼠标
  - 自定义十字辅助点在所有屏幕（含高 DPI 主屏）下均完整显示

### 修复

- **OKLCh 的 L 值条**：滑块联动不再错位 / 跳变

---

## v1.1.1

Fix: grayscale overlay (OpenGL / Ctrl+G) broken after PyInstaller packaging.

### Root Cause
dxcam dynamically imports OpenCV via `importlib.import_module("cv2")` which PyInstaller's static analysis cannot detect. This causes `ModuleNotFoundError: No module named 'cv2'` at runtime, crashing the frame processing thread.

Additional issues: `GrayscaleOverlay` missing `is_healthy` attribute (AttributeError), dxcam C extension (`_numpy_kernels.pyd`) not bundled, and overlay C++ EXEs not included.

### Changes
- **ui/grayscale_overlay.py**: Added `is_healthy` property + QTimer-based delayed health check (2.5s after overlay creation)
- **core/dcomp_grayscale.py**: Fixed DComp EXE path lookup for PyInstaller-frozen environments (`sys._MEIPASS`)
- **PyInstaller specs**: Added hidden imports (`cv2`, all dxcam submodules, `comtypes`), explicitly bundled dxcam C extension `.pyd` and `dcomp_overlay.exe`/`sc_overlay.exe`
- **build_pyqt.py**: Now builds both onedir (folder) and onefile (single EXE) outputs simultaneously
